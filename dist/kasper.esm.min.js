class U {
  constructor(t) {
    if (this.args = {}, this.$abortController = new AbortController(), !t) {
      this.args = {};
      return;
    }
    t.args && (this.args = t.args || {}), t.ref && (this.ref = t.ref), t.transpiler && (this.transpiler = t.transpiler);
  }
  onInit() {
  }
  onRender() {
  }
  onChanges() {
  }
  onDestroy() {
  }
  $doRender() {
    this.transpiler;
  }
}
class Z extends Error {
  constructor(t, e, s) {
    super(`Parse Error (${e}:${s}) => ${t}`), this.name = "KasperError", this.line = e, this.col = s;
  }
}
class u {
  // tslint:disable-next-line
  constructor() {
  }
}
class W extends u {
  constructor(t, e, s) {
    super(), this.params = t, this.body = e, this.line = s;
  }
  accept(t) {
    return t.visitArrowFunctionExpr(this);
  }
  toString() {
    return "Expr.ArrowFunction";
  }
}
class nt extends u {
  constructor(t, e, s) {
    super(), this.name = t, this.value = e, this.line = s;
  }
  accept(t) {
    return t.visitAssignExpr(this);
  }
  toString() {
    return "Expr.Assign";
  }
}
class $ extends u {
  constructor(t, e, s, n) {
    super(), this.left = t, this.operator = e, this.right = s, this.line = n;
  }
  accept(t) {
    return t.visitBinaryExpr(this);
  }
  toString() {
    return "Expr.Binary";
  }
}
class X extends u {
  constructor(t, e, s, n, c = !1) {
    super(), this.callee = t, this.paren = e, this.args = s, this.line = n, this.optional = c;
  }
  accept(t) {
    return t.visitCallExpr(this);
  }
  toString() {
    return "Expr.Call";
  }
}
class at extends u {
  constructor(t, e) {
    super(), this.value = t, this.line = e;
  }
  accept(t) {
    return t.visitDebugExpr(this);
  }
  toString() {
    return "Expr.Debug";
  }
}
class j extends u {
  constructor(t, e) {
    super(), this.properties = t, this.line = e;
  }
  accept(t) {
    return t.visitDictionaryExpr(this);
  }
  toString() {
    return "Expr.Dictionary";
  }
}
class ct extends u {
  constructor(t, e, s, n) {
    super(), this.name = t, this.key = e, this.iterable = s, this.line = n;
  }
  accept(t) {
    return t.visitEachExpr(this);
  }
  toString() {
    return "Expr.Each";
  }
}
class S extends u {
  constructor(t, e, s, n) {
    super(), this.entity = t, this.key = e, this.type = s, this.line = n;
  }
  accept(t) {
    return t.visitGetExpr(this);
  }
  toString() {
    return "Expr.Get";
  }
}
class ht extends u {
  constructor(t, e) {
    super(), this.expression = t, this.line = e;
  }
  accept(t) {
    return t.visitGroupingExpr(this);
  }
  toString() {
    return "Expr.Grouping";
  }
}
class M extends u {
  constructor(t, e) {
    super(), this.name = t, this.line = e;
  }
  accept(t) {
    return t.visitKeyExpr(this);
  }
  toString() {
    return "Expr.Key";
  }
}
class z extends u {
  constructor(t, e, s, n) {
    super(), this.left = t, this.operator = e, this.right = s, this.line = n;
  }
  accept(t) {
    return t.visitLogicalExpr(this);
  }
  toString() {
    return "Expr.Logical";
  }
}
class _ extends u {
  constructor(t, e) {
    super(), this.value = t, this.line = e;
  }
  accept(t) {
    return t.visitListExpr(this);
  }
  toString() {
    return "Expr.List";
  }
}
class q extends u {
  constructor(t, e) {
    super(), this.value = t, this.line = e;
  }
  accept(t) {
    return t.visitLiteralExpr(this);
  }
  toString() {
    return "Expr.Literal";
  }
}
class ot extends u {
  constructor(t, e) {
    super(), this.clazz = t, this.line = e;
  }
  accept(t) {
    return t.visitNewExpr(this);
  }
  toString() {
    return "Expr.New";
  }
}
class lt extends u {
  constructor(t, e, s) {
    super(), this.left = t, this.right = e, this.line = s;
  }
  accept(t) {
    return t.visitNullCoalescingExpr(this);
  }
  toString() {
    return "Expr.NullCoalescing";
  }
}
class F extends u {
  constructor(t, e, s) {
    super(), this.entity = t, this.increment = e, this.line = s;
  }
  accept(t) {
    return t.visitPostfixExpr(this);
  }
  toString() {
    return "Expr.Postfix";
  }
}
let N = class extends u {
  constructor(t, e, s, n) {
    super(), this.entity = t, this.key = e, this.value = s, this.line = n;
  }
  accept(t) {
    return t.visitSetExpr(this);
  }
  toString() {
    return "Expr.Set";
  }
};
class ut extends u {
  constructor(t, e, s) {
    super(), this.left = t, this.right = e, this.line = s;
  }
  accept(t) {
    return t.visitPipelineExpr(this);
  }
  toString() {
    return "Expr.Pipeline";
  }
}
class k extends u {
  constructor(t, e) {
    super(), this.value = t, this.line = e;
  }
  accept(t) {
    return t.visitSpreadExpr(this);
  }
  toString() {
    return "Expr.Spread";
  }
}
class ft extends u {
  constructor(t, e) {
    super(), this.value = t, this.line = e;
  }
  accept(t) {
    return t.visitTemplateExpr(this);
  }
  toString() {
    return "Expr.Template";
  }
}
class pt extends u {
  constructor(t, e, s, n) {
    super(), this.condition = t, this.thenExpr = e, this.elseExpr = s, this.line = n;
  }
  accept(t) {
    return t.visitTernaryExpr(this);
  }
  toString() {
    return "Expr.Ternary";
  }
}
class dt extends u {
  constructor(t, e) {
    super(), this.value = t, this.line = e;
  }
  accept(t) {
    return t.visitTypeofExpr(this);
  }
  toString() {
    return "Expr.Typeof";
  }
}
class mt extends u {
  constructor(t, e, s) {
    super(), this.operator = t, this.right = e, this.line = s;
  }
  accept(t) {
    return t.visitUnaryExpr(this);
  }
  toString() {
    return "Expr.Unary";
  }
}
class A extends u {
  constructor(t, e) {
    super(), this.name = t, this.line = e;
  }
  accept(t) {
    return t.visitVariableExpr(this);
  }
  toString() {
    return "Expr.Variable";
  }
}
class vt extends u {
  constructor(t, e) {
    super(), this.value = t, this.line = e;
  }
  accept(t) {
    return t.visitVoidExpr(this);
  }
  toString() {
    return "Expr.Void";
  }
}
var r = /* @__PURE__ */ ((i) => (i[i.Eof = 0] = "Eof", i[i.Panic = 1] = "Panic", i[i.Ampersand = 2] = "Ampersand", i[i.AtSign = 3] = "AtSign", i[i.Caret = 4] = "Caret", i[i.Comma = 5] = "Comma", i[i.Dollar = 6] = "Dollar", i[i.Dot = 7] = "Dot", i[i.Hash = 8] = "Hash", i[i.LeftBrace = 9] = "LeftBrace", i[i.LeftBracket = 10] = "LeftBracket", i[i.LeftParen = 11] = "LeftParen", i[i.Percent = 12] = "Percent", i[i.Pipe = 13] = "Pipe", i[i.RightBrace = 14] = "RightBrace", i[i.RightBracket = 15] = "RightBracket", i[i.RightParen = 16] = "RightParen", i[i.Semicolon = 17] = "Semicolon", i[i.Slash = 18] = "Slash", i[i.Star = 19] = "Star", i[i.Arrow = 20] = "Arrow", i[i.Bang = 21] = "Bang", i[i.BangEqual = 22] = "BangEqual", i[i.BangEqualEqual = 23] = "BangEqualEqual", i[i.Colon = 24] = "Colon", i[i.Equal = 25] = "Equal", i[i.EqualEqual = 26] = "EqualEqual", i[i.EqualEqualEqual = 27] = "EqualEqualEqual", i[i.Greater = 28] = "Greater", i[i.GreaterEqual = 29] = "GreaterEqual", i[i.Less = 30] = "Less", i[i.LessEqual = 31] = "LessEqual", i[i.Minus = 32] = "Minus", i[i.MinusEqual = 33] = "MinusEqual", i[i.MinusMinus = 34] = "MinusMinus", i[i.PercentEqual = 35] = "PercentEqual", i[i.Plus = 36] = "Plus", i[i.PlusEqual = 37] = "PlusEqual", i[i.PlusPlus = 38] = "PlusPlus", i[i.Question = 39] = "Question", i[i.QuestionDot = 40] = "QuestionDot", i[i.QuestionQuestion = 41] = "QuestionQuestion", i[i.SlashEqual = 42] = "SlashEqual", i[i.StarEqual = 43] = "StarEqual", i[i.DotDot = 44] = "DotDot", i[i.DotDotDot = 45] = "DotDotDot", i[i.LessEqualGreater = 46] = "LessEqualGreater", i[i.Identifier = 47] = "Identifier", i[i.Template = 48] = "Template", i[i.String = 49] = "String", i[i.Number = 50] = "Number", i[i.LeftShift = 51] = "LeftShift", i[i.RightShift = 52] = "RightShift", i[i.Pipeline = 53] = "Pipeline", i[i.Tilde = 54] = "Tilde", i[i.And = 55] = "And", i[i.Const = 56] = "Const", i[i.Debug = 57] = "Debug", i[i.False = 58] = "False", i[i.In = 59] = "In", i[i.Instanceof = 60] = "Instanceof", i[i.New = 61] = "New", i[i.Null = 62] = "Null", i[i.Undefined = 63] = "Undefined", i[i.Of = 64] = "Of", i[i.Or = 65] = "Or", i[i.True = 66] = "True", i[i.Typeof = 67] = "Typeof", i[i.Void = 68] = "Void", i[i.With = 69] = "With", i))(r || {});
class H {
  constructor(t, e, s, n, c) {
    this.name = r[t], this.type = t, this.lexeme = e, this.literal = s, this.line = n, this.col = c;
  }
  toString() {
    return `[(${this.line}):"${this.lexeme}"]`;
  }
}
const J = [" ", `
`, "	", "\r"], gt = [
  "area",
  "base",
  "br",
  "col",
  "embed",
  "hr",
  "img",
  "input",
  "link",
  "meta",
  "param",
  "source",
  "track",
  "wbr"
];
class T {
  parse(t) {
    this.current = 0, this.tokens = t;
    const e = [];
    for (; !this.eof(); )
      e.push(this.expression());
    return e;
  }
  match(...t) {
    for (const e of t)
      if (this.check(e))
        return this.advance(), !0;
    return !1;
  }
  advance() {
    return this.eof() || this.current++, this.previous();
  }
  peek() {
    return this.tokens[this.current];
  }
  previous() {
    return this.tokens[this.current - 1];
  }
  check(t) {
    return this.peek().type === t;
  }
  eof() {
    return this.check(r.Eof);
  }
  consume(t, e) {
    return this.check(t) ? this.advance() : this.error(
      this.peek(),
      e + `, unexpected token "${this.peek().lexeme}"`
    );
  }
  error(t, e) {
    throw new Z(e, t.line, t.col);
  }
  synchronize() {
    do {
      if (this.check(r.Semicolon) || this.check(r.RightBrace)) {
        this.advance();
        return;
      }
      this.advance();
    } while (!this.eof());
  }
  foreach(t) {
    this.current = 0, this.tokens = t;
    const e = this.consume(
      r.Identifier,
      'Expected an identifier inside "each" statement'
    );
    let s = null;
    this.match(r.With) && (s = this.consume(
      r.Identifier,
      'Expected a "key" identifier after "with" keyword in foreach statement'
    )), this.consume(
      r.Of,
      'Expected "of" keyword inside foreach statement'
    );
    const n = this.expression();
    return new ct(e, s, n, e.line);
  }
  expression() {
    const t = this.assignment();
    if (this.match(r.Semicolon))
      for (; this.match(r.Semicolon); )
        ;
    return t;
  }
  assignment() {
    const t = this.pipeline();
    if (this.match(
      r.Equal,
      r.PlusEqual,
      r.MinusEqual,
      r.StarEqual,
      r.SlashEqual
    )) {
      const e = this.previous();
      let s = this.assignment();
      if (t instanceof A) {
        const n = t.name;
        return e.type !== r.Equal && (s = new $(
          new A(n, n.line),
          e,
          s,
          e.line
        )), new nt(n, s, n.line);
      } else if (t instanceof S)
        return e.type !== r.Equal && (s = new $(
          new S(t.entity, t.key, t.type, t.line),
          e,
          s,
          e.line
        )), new N(t.entity, t.key, s, t.line);
      this.error(e, "Invalid l-value, is not an assigning target.");
    }
    return t;
  }
  pipeline() {
    let t = this.ternary();
    for (; this.match(r.Pipeline); ) {
      const e = this.ternary();
      t = new ut(t, e, t.line);
    }
    return t;
  }
  ternary() {
    const t = this.nullCoalescing();
    if (this.match(r.Question)) {
      const e = this.ternary();
      this.consume(r.Colon, 'Expected ":" after ternary ? expression');
      const s = this.ternary();
      return new pt(t, e, s, t.line);
    }
    return t;
  }
  nullCoalescing() {
    const t = this.logicalOr();
    if (this.match(r.QuestionQuestion)) {
      const e = this.nullCoalescing();
      return new lt(t, e, t.line);
    }
    return t;
  }
  logicalOr() {
    let t = this.logicalAnd();
    for (; this.match(r.Or); ) {
      const e = this.previous(), s = this.logicalAnd();
      t = new z(t, e, s, e.line);
    }
    return t;
  }
  logicalAnd() {
    let t = this.equality();
    for (; this.match(r.And); ) {
      const e = this.previous(), s = this.equality();
      t = new z(t, e, s, e.line);
    }
    return t;
  }
  equality() {
    let t = this.shift();
    for (; this.match(
      r.BangEqual,
      r.BangEqualEqual,
      r.EqualEqual,
      r.EqualEqualEqual,
      r.Greater,
      r.GreaterEqual,
      r.Less,
      r.LessEqual,
      r.Instanceof,
      r.In
    ); ) {
      const e = this.previous(), s = this.shift();
      t = new $(t, e, s, e.line);
    }
    return t;
  }
  shift() {
    let t = this.addition();
    for (; this.match(r.LeftShift, r.RightShift); ) {
      const e = this.previous(), s = this.addition();
      t = new $(t, e, s, e.line);
    }
    return t;
  }
  addition() {
    let t = this.modulus();
    for (; this.match(r.Minus, r.Plus); ) {
      const e = this.previous(), s = this.modulus();
      t = new $(t, e, s, e.line);
    }
    return t;
  }
  modulus() {
    let t = this.multiplication();
    for (; this.match(r.Percent); ) {
      const e = this.previous(), s = this.multiplication();
      t = new $(t, e, s, e.line);
    }
    return t;
  }
  multiplication() {
    let t = this.typeof();
    for (; this.match(r.Slash, r.Star); ) {
      const e = this.previous(), s = this.typeof();
      t = new $(t, e, s, e.line);
    }
    return t;
  }
  typeof() {
    if (this.match(r.Typeof)) {
      const t = this.previous(), e = this.typeof();
      return new dt(e, t.line);
    }
    return this.unary();
  }
  unary() {
    if (this.match(
      r.Minus,
      r.Bang,
      r.Tilde,
      r.Dollar,
      r.PlusPlus,
      r.MinusMinus
    )) {
      const t = this.previous(), e = this.unary();
      return new mt(t, e, t.line);
    }
    return this.newKeyword();
  }
  newKeyword() {
    if (this.match(r.New)) {
      const t = this.previous(), e = this.postfix();
      return new ot(e, t.line);
    }
    return this.postfix();
  }
  postfix() {
    const t = this.call();
    return this.match(r.PlusPlus) ? new F(t, 1, t.line) : this.match(r.MinusMinus) ? new F(t, -1, t.line) : t;
  }
  call() {
    let t = this.primary(), e;
    do {
      if (e = !1, this.match(r.LeftParen)) {
        e = !0;
        do
          t = this.finishCall(t, this.previous(), !1);
        while (this.match(r.LeftParen));
      }
      if (this.match(r.Dot, r.QuestionDot)) {
        e = !0;
        const s = this.previous();
        s.type === r.QuestionDot && this.match(r.LeftBracket) ? t = this.bracketGet(t, s) : s.type === r.QuestionDot && this.match(r.LeftParen) ? t = this.finishCall(t, this.previous(), !0) : t = this.dotGet(t, s);
      }
      this.match(r.LeftBracket) && (e = !0, t = this.bracketGet(t, this.previous()));
    } while (e);
    return t;
  }
  tokenAt(t) {
    var e;
    return (e = this.tokens[this.current + t]) == null ? void 0 : e.type;
  }
  isArrowParams() {
    var e, s, n, c, a, h;
    let t = this.current + 1;
    if (((e = this.tokens[t]) == null ? void 0 : e.type) === r.RightParen)
      return ((s = this.tokens[t + 1]) == null ? void 0 : s.type) === r.Arrow;
    for (; t < this.tokens.length; ) {
      if (((n = this.tokens[t]) == null ? void 0 : n.type) !== r.Identifier) return !1;
      if (t++, ((c = this.tokens[t]) == null ? void 0 : c.type) === r.RightParen)
        return ((a = this.tokens[t + 1]) == null ? void 0 : a.type) === r.Arrow;
      if (((h = this.tokens[t]) == null ? void 0 : h.type) !== r.Comma) return !1;
      t++;
    }
    return !1;
  }
  finishCall(t, e, s) {
    const n = [];
    if (!this.check(r.RightParen))
      do
        this.match(r.DotDotDot) ? n.push(new k(this.expression(), this.previous().line)) : n.push(this.expression());
      while (this.match(r.Comma));
    const c = this.consume(r.RightParen, 'Expected ")" after arguments');
    return new X(t, c, n, c.line, s);
  }
  dotGet(t, e) {
    const s = this.consume(
      r.Identifier,
      "Expect property name after '.'"
    ), n = new M(s, s.line);
    return new S(t, n, e.type, s.line);
  }
  bracketGet(t, e) {
    let s = null;
    return this.check(r.RightBracket) || (s = this.expression()), this.consume(r.RightBracket, 'Expected "]" after an index'), new S(t, s, e.type, e.line);
  }
  primary() {
    if (this.match(r.False))
      return new q(!1, this.previous().line);
    if (this.match(r.True))
      return new q(!0, this.previous().line);
    if (this.match(r.Null))
      return new q(null, this.previous().line);
    if (this.match(r.Undefined))
      return new q(void 0, this.previous().line);
    if (this.match(r.Number) || this.match(r.String))
      return new q(this.previous().literal, this.previous().line);
    if (this.match(r.Template))
      return new ft(this.previous().literal, this.previous().line);
    if (this.check(r.Identifier) && this.tokenAt(1) === r.Arrow) {
      const t = this.advance();
      this.advance();
      const e = this.expression();
      return new W([t], e, t.line);
    }
    if (this.match(r.Identifier)) {
      const t = this.previous();
      return new A(t, t.line);
    }
    if (this.check(r.LeftParen) && this.isArrowParams()) {
      this.advance();
      const t = [];
      if (!this.check(r.RightParen))
        do
          t.push(this.consume(r.Identifier, "Expected parameter name"));
        while (this.match(r.Comma));
      this.consume(r.RightParen, 'Expected ")"'), this.consume(r.Arrow, 'Expected "=>"');
      const e = this.expression();
      return new W(t, e, this.previous().line);
    }
    if (this.match(r.LeftParen)) {
      const t = this.expression();
      return this.consume(r.RightParen, 'Expected ")" after expression'), new ht(t, t.line);
    }
    if (this.match(r.LeftBrace))
      return this.dictionary();
    if (this.match(r.LeftBracket))
      return this.list();
    if (this.match(r.Void)) {
      const t = this.expression();
      return new vt(t, this.previous().line);
    }
    if (this.match(r.Debug)) {
      const t = this.expression();
      return new at(t, this.previous().line);
    }
    throw this.error(
      this.peek(),
      `Expected expression, unexpected token "${this.peek().lexeme}"`
    );
  }
  dictionary() {
    const t = this.previous();
    if (this.match(r.RightBrace))
      return new j([], this.previous().line);
    const e = [];
    do
      if (this.match(r.DotDotDot))
        e.push(new k(this.expression(), this.previous().line));
      else if (this.match(r.String, r.Identifier, r.Number)) {
        const s = this.previous();
        if (this.match(r.Colon)) {
          const n = this.expression();
          e.push(
            new N(null, new M(s, s.line), n, s.line)
          );
        } else {
          const n = new A(s, s.line);
          e.push(
            new N(null, new M(s, s.line), n, s.line)
          );
        }
      } else
        this.error(
          this.peek(),
          `String, Number or Identifier expected as a Key of Dictionary {, unexpected token ${this.peek().lexeme}`
        );
    while (this.match(r.Comma));
    return this.consume(r.RightBrace, 'Expected "}" after object literal'), new j(e, t.line);
  }
  list() {
    const t = [], e = this.previous();
    if (this.match(r.RightBracket))
      return new _([], this.previous().line);
    do
      this.match(r.DotDotDot) ? t.push(new k(this.expression(), this.previous().line)) : t.push(this.expression());
    while (this.match(r.Comma));
    return this.consume(
      r.RightBracket,
      'Expected "]" after array declaration'
    ), new _(t, e.line);
  }
}
function P(i) {
  return i >= "0" && i <= "9";
}
function tt(i) {
  return i >= "a" && i <= "z" || i >= "A" && i <= "Z" || i === "$" || i === "_";
}
function Et(i) {
  return tt(i) || P(i);
}
function wt(i) {
  return i.charAt(0).toUpperCase() + i.substring(1).toLowerCase();
}
function xt(i) {
  return r[i] >= r.And;
}
class et {
  scan(t) {
    for (this.source = t, this.tokens = [], this.current = 0, this.start = 0, this.line = 1, this.col = 1; !this.eof(); )
      this.start = this.current, this.getToken();
    return this.tokens.push(new H(r.Eof, "", null, this.line, 0)), this.tokens;
  }
  eof() {
    return this.current >= this.source.length;
  }
  advance() {
    return this.peek() === `
` && (this.line++, this.col = 0), this.current++, this.col++, this.source.charAt(this.current - 1);
  }
  addToken(t, e) {
    const s = this.source.substring(this.start, this.current);
    this.tokens.push(new H(t, s, e, this.line, this.col));
  }
  match(t) {
    return this.eof() || this.source.charAt(this.current) !== t ? !1 : (this.current++, !0);
  }
  peek() {
    return this.eof() ? "\0" : this.source.charAt(this.current);
  }
  peekNext() {
    return this.current + 1 >= this.source.length ? "\0" : this.source.charAt(this.current + 1);
  }
  comment() {
    for (; this.peek() !== `
` && !this.eof(); )
      this.advance();
  }
  multilineComment() {
    for (; !this.eof() && !(this.peek() === "*" && this.peekNext() === "/"); )
      this.advance();
    this.eof() ? this.error('Unterminated comment, expecting closing "*/"') : (this.advance(), this.advance());
  }
  string(t) {
    for (; this.peek() !== t && !this.eof(); )
      this.advance();
    if (this.eof()) {
      this.error(`Unterminated string, expecting closing ${t}`);
      return;
    }
    this.advance();
    const e = this.source.substring(this.start + 1, this.current - 1);
    this.addToken(t !== "`" ? r.String : r.Template, e);
  }
  number() {
    for (; P(this.peek()); )
      this.advance();
    for (this.peek() === "." && P(this.peekNext()) && this.advance(); P(this.peek()); )
      this.advance();
    for (this.peek().toLowerCase() === "e" && (this.advance(), (this.peek() === "-" || this.peek() === "+") && this.advance()); P(this.peek()); )
      this.advance();
    const t = this.source.substring(this.start, this.current);
    this.addToken(r.Number, Number(t));
  }
  identifier() {
    for (; Et(this.peek()); )
      this.advance();
    const t = this.source.substring(this.start, this.current), e = wt(t);
    xt(e) ? this.addToken(r[e], t) : this.addToken(r.Identifier, t);
  }
  getToken() {
    const t = this.advance();
    switch (t) {
      case "(":
        this.addToken(r.LeftParen, null);
        break;
      case ")":
        this.addToken(r.RightParen, null);
        break;
      case "[":
        this.addToken(r.LeftBracket, null);
        break;
      case "]":
        this.addToken(r.RightBracket, null);
        break;
      case "{":
        this.addToken(r.LeftBrace, null);
        break;
      case "}":
        this.addToken(r.RightBrace, null);
        break;
      case ",":
        this.addToken(r.Comma, null);
        break;
      case ";":
        this.addToken(r.Semicolon, null);
        break;
      case "~":
        this.addToken(r.Tilde, null);
        break;
      case "^":
        this.addToken(r.Caret, null);
        break;
      case "#":
        this.addToken(r.Hash, null);
        break;
      case ":":
        this.addToken(
          this.match("=") ? r.Arrow : r.Colon,
          null
        );
        break;
      case "*":
        this.addToken(
          this.match("=") ? r.StarEqual : r.Star,
          null
        );
        break;
      case "%":
        this.addToken(
          this.match("=") ? r.PercentEqual : r.Percent,
          null
        );
        break;
      case "|":
        this.addToken(
          this.match("|") ? r.Or : this.match(">") ? r.Pipeline : r.Pipe,
          null
        );
        break;
      case "&":
        this.addToken(
          this.match("&") ? r.And : r.Ampersand,
          null
        );
        break;
      case ">":
        this.addToken(
          this.match(">") ? r.RightShift : this.match("=") ? r.GreaterEqual : r.Greater,
          null
        );
        break;
      case "!":
        this.addToken(
          this.match("=") ? this.match("=") ? r.BangEqualEqual : r.BangEqual : r.Bang,
          null
        );
        break;
      case "?":
        this.addToken(
          this.match("?") ? r.QuestionQuestion : this.match(".") ? r.QuestionDot : r.Question,
          null
        );
        break;
      case "=":
        if (this.match("=")) {
          this.addToken(
            this.match("=") ? r.EqualEqualEqual : r.EqualEqual,
            null
          );
          break;
        }
        this.addToken(
          this.match(">") ? r.Arrow : r.Equal,
          null
        );
        break;
      case "+":
        this.addToken(
          this.match("+") ? r.PlusPlus : this.match("=") ? r.PlusEqual : r.Plus,
          null
        );
        break;
      case "-":
        this.addToken(
          this.match("-") ? r.MinusMinus : this.match("=") ? r.MinusEqual : r.Minus,
          null
        );
        break;
      case "<":
        this.addToken(
          this.match("<") ? r.LeftShift : this.match("=") ? this.match(">") ? r.LessEqualGreater : r.LessEqual : r.Less,
          null
        );
        break;
      case ".":
        this.match(".") ? this.match(".") ? this.addToken(r.DotDotDot, null) : this.addToken(r.DotDot, null) : this.addToken(r.Dot, null);
        break;
      case "/":
        this.match("/") ? this.comment() : this.match("*") ? this.multilineComment() : this.addToken(
          this.match("=") ? r.SlashEqual : r.Slash,
          null
        );
        break;
      case "'":
      case '"':
      case "`":
        this.string(t);
        break;
      // ignore cases
      case `
`:
      case " ":
      case "\r":
      case "	":
        break;
      // complex cases
      default:
        P(t) ? this.number() : tt(t) ? this.identifier() : this.error(`Unexpected character '${t}'`);
        break;
    }
  }
  error(t) {
    throw new Error(`Scan Error (${this.line}:${this.col}) => ${t}`);
  }
}
class b {
  constructor(t, e) {
    this.parent = t || null, this.values = e || {};
  }
  init(t) {
    this.values = t || {};
  }
  set(t, e) {
    this.values[t] = e;
  }
  get(t) {
    return typeof this.values[t] < "u" ? this.values[t] : this.parent !== null ? this.parent.get(t) : window[t];
  }
}
class bt {
  constructor() {
    this.scope = new b(), this.scanner = new et(), this.parser = new T();
  }
  evaluate(t) {
    return t.result = t.accept(this);
  }
  visitPipelineExpr(t) {
    const e = this.evaluate(t.left);
    if (t.right instanceof X) {
      const n = this.evaluate(t.right.callee), c = [e];
      for (const a of t.right.args)
        a instanceof k ? c.push(...this.evaluate(a.value)) : c.push(this.evaluate(a));
      return t.right.callee instanceof S ? n.apply(t.right.callee.entity.result, c) : n(...c);
    }
    return this.evaluate(t.right)(e);
  }
  visitArrowFunctionExpr(t) {
    const e = this.scope;
    return (...s) => {
      const n = this.scope;
      this.scope = new b(e);
      for (let c = 0; c < t.params.length; c++)
        this.scope.set(t.params[c].lexeme, s[c]);
      try {
        return this.evaluate(t.body);
      } finally {
        this.scope = n;
      }
    };
  }
  error(t) {
    throw new Error(`Runtime Error => ${t}`);
  }
  visitVariableExpr(t) {
    return this.scope.get(t.name.lexeme);
  }
  visitAssignExpr(t) {
    const e = this.evaluate(t.value);
    return this.scope.set(t.name.lexeme, e), e;
  }
  visitKeyExpr(t) {
    return t.name.literal;
  }
  visitGetExpr(t) {
    const e = this.evaluate(t.entity), s = this.evaluate(t.key);
    if (!(!e && t.type === r.QuestionDot))
      return e[s];
  }
  visitSetExpr(t) {
    const e = this.evaluate(t.entity), s = this.evaluate(t.key), n = this.evaluate(t.value);
    return e[s] = n, n;
  }
  visitPostfixExpr(t) {
    const e = this.evaluate(t.entity), s = e + t.increment;
    if (t.entity instanceof A)
      this.scope.set(t.entity.name.lexeme, s);
    else if (t.entity instanceof S) {
      const n = new N(
        t.entity.entity,
        t.entity.key,
        new q(s, t.line),
        t.line
      );
      this.evaluate(n);
    } else
      this.error(`Invalid left-hand side in postfix operation: ${t.entity}`);
    return e;
  }
  visitListExpr(t) {
    const e = [];
    for (const s of t.value)
      s instanceof k ? e.push(...this.evaluate(s.value)) : e.push(this.evaluate(s));
    return e;
  }
  visitSpreadExpr(t) {
    return this.evaluate(t.value);
  }
  templateParse(t) {
    const e = this.scanner.scan(t), s = this.parser.parse(e);
    let n = "";
    for (const c of s)
      n += this.evaluate(c).toString();
    return n;
  }
  visitTemplateExpr(t) {
    return t.value.replace(
      /\{\{([\s\S]+?)\}\}/g,
      (s, n) => this.templateParse(n)
    );
  }
  visitBinaryExpr(t) {
    const e = this.evaluate(t.left), s = this.evaluate(t.right);
    switch (t.operator.type) {
      case r.Minus:
      case r.MinusEqual:
        return e - s;
      case r.Slash:
      case r.SlashEqual:
        return e / s;
      case r.Star:
      case r.StarEqual:
        return e * s;
      case r.Percent:
      case r.PercentEqual:
        return e % s;
      case r.Plus:
      case r.PlusEqual:
        return e + s;
      case r.Pipe:
        return e | s;
      case r.Caret:
        return e ^ s;
      case r.Greater:
        return e > s;
      case r.GreaterEqual:
        return e >= s;
      case r.Less:
        return e < s;
      case r.LessEqual:
        return e <= s;
      case r.EqualEqual:
      case r.EqualEqualEqual:
        return e === s;
      case r.BangEqual:
      case r.BangEqualEqual:
        return e !== s;
      case r.Instanceof:
        return e instanceof s;
      case r.In:
        return e in s;
      case r.LeftShift:
        return e << s;
      case r.RightShift:
        return e >> s;
      default:
        return this.error("Unknown binary operator " + t.operator), null;
    }
  }
  visitLogicalExpr(t) {
    const e = this.evaluate(t.left);
    if (t.operator.type === r.Or) {
      if (e)
        return e;
    } else if (!e)
      return e;
    return this.evaluate(t.right);
  }
  visitTernaryExpr(t) {
    return this.evaluate(t.condition) ? this.evaluate(t.thenExpr) : this.evaluate(t.elseExpr);
  }
  visitNullCoalescingExpr(t) {
    const e = this.evaluate(t.left);
    return e ?? this.evaluate(t.right);
  }
  visitGroupingExpr(t) {
    return this.evaluate(t.expression);
  }
  visitLiteralExpr(t) {
    return t.value;
  }
  visitUnaryExpr(t) {
    const e = this.evaluate(t.right);
    switch (t.operator.type) {
      case r.Minus:
        return -e;
      case r.Bang:
        return !e;
      case r.Tilde:
        return ~e;
      case r.PlusPlus:
      case r.MinusMinus: {
        const s = Number(e) + (t.operator.type === r.PlusPlus ? 1 : -1);
        if (t.right instanceof A)
          this.scope.set(t.right.name.lexeme, s);
        else if (t.right instanceof S) {
          const n = new N(
            t.right.entity,
            t.right.key,
            new q(s, t.line),
            t.line
          );
          this.evaluate(n);
        } else
          this.error(
            `Invalid right-hand side expression in prefix operation:  ${t.right}`
          );
        return s;
      }
      default:
        return this.error("Unknown unary operator ' + expr.operator"), null;
    }
  }
  visitCallExpr(t) {
    const e = this.evaluate(t.callee);
    if (e == null && t.optional) return;
    typeof e != "function" && this.error(`${e} is not a function`);
    const s = [];
    for (const n of t.args)
      n instanceof k ? s.push(...this.evaluate(n.value)) : s.push(this.evaluate(n));
    return t.callee instanceof S ? e.apply(t.callee.entity.result, s) : e(...s);
  }
  visitNewExpr(t) {
    const e = t.clazz, s = this.evaluate(e.callee);
    typeof s != "function" && this.error(
      `'${s}' is not a class. 'new' statement must be used with classes.`
    );
    const n = [];
    for (const c of e.args)
      n.push(this.evaluate(c));
    return new s(...n);
  }
  visitDictionaryExpr(t) {
    const e = {};
    for (const s of t.properties)
      if (s instanceof k)
        Object.assign(e, this.evaluate(s.value));
      else {
        const n = this.evaluate(s.key), c = this.evaluate(s.value);
        e[n] = c;
      }
    return e;
  }
  visitTypeofExpr(t) {
    return typeof this.evaluate(t.value);
  }
  visitEachExpr(t) {
    return [
      t.name.lexeme,
      t.key ? t.key.lexeme : null,
      this.evaluate(t.iterable)
    ];
  }
  visitVoidExpr(t) {
    return this.evaluate(t.value), "";
  }
  visitDebugExpr(t) {
    const e = this.evaluate(t.value);
    return console.log(e), "";
  }
}
class B {
}
class Y extends B {
  constructor(t, e, s, n, c = 0) {
    super(), this.type = "element", this.name = t, this.attributes = e, this.children = s, this.self = n, this.line = c;
  }
  accept(t, e) {
    return t.visitElementKNode(this, e);
  }
  toString() {
    return "KNode.Element";
  }
}
class St extends B {
  constructor(t, e, s = 0) {
    super(), this.type = "attribute", this.name = t, this.value = e, this.line = s;
  }
  accept(t, e) {
    return t.visitAttributeKNode(this, e);
  }
  toString() {
    return "KNode.Attribute";
  }
}
class yt extends B {
  constructor(t, e = 0) {
    super(), this.type = "text", this.value = t, this.line = e;
  }
  accept(t, e) {
    return t.visitTextKNode(this, e);
  }
  toString() {
    return "KNode.Text";
  }
}
let $t = class extends B {
  constructor(t, e = 0) {
    super(), this.type = "comment", this.value = t, this.line = e;
  }
  accept(t, e) {
    return t.visitCommentKNode(this, e);
  }
  toString() {
    return "KNode.Comment";
  }
};
class qt extends B {
  constructor(t, e = 0) {
    super(), this.type = "doctype", this.value = t, this.line = e;
  }
  accept(t, e) {
    return t.visitDoctypeKNode(this, e);
  }
  toString() {
    return "KNode.Doctype";
  }
}
class I {
  parse(t) {
    for (this.current = 0, this.line = 1, this.col = 1, this.source = t, this.nodes = []; !this.eof(); ) {
      const e = this.node();
      e !== null && this.nodes.push(e);
    }
    return this.source = "", this.nodes;
  }
  match(...t) {
    for (const e of t)
      if (this.check(e))
        return this.current += e.length, !0;
    return !1;
  }
  advance(t = "") {
    this.eof() ? this.error(`Unexpected end of file. ${t}`) : (this.check(`
`) && (this.line += 1, this.col = 0), this.col += 1, this.current++);
  }
  peek(...t) {
    for (const e of t)
      if (this.check(e))
        return !0;
    return !1;
  }
  check(t) {
    return this.source.slice(this.current, this.current + t.length) === t;
  }
  eof() {
    return this.current > this.source.length;
  }
  error(t) {
    throw new Z(t, this.line, this.col);
  }
  node() {
    this.whitespace();
    let t;
    return this.match("</") && this.error("Unexpected closing tag"), this.match("<!--") ? t = this.comment() : this.match("<!doctype") || this.match("<!DOCTYPE") ? t = this.doctype() : this.match("<") ? t = this.element() : t = this.text(), this.whitespace(), t;
  }
  comment() {
    const t = this.current;
    do
      this.advance("Expected comment closing '-->'");
    while (!this.match("-->"));
    const e = this.source.slice(t, this.current - 3);
    return new $t(e, this.line);
  }
  doctype() {
    const t = this.current;
    do
      this.advance("Expected closing doctype");
    while (!this.match(">"));
    const e = this.source.slice(t, this.current - 1).trim();
    return new qt(e, this.line);
  }
  element() {
    const t = this.line, e = this.identifier("/", ">");
    e || this.error("Expected a tag name");
    const s = this.attributes();
    if (this.match("/>") || gt.includes(e) && this.match(">"))
      return new Y(e, s, [], !0, this.line);
    this.match(">") || this.error("Expected closing tag");
    let n = [];
    return this.whitespace(), this.peek("</") || (n = this.children(e)), this.close(e), new Y(e, s, n, !1, t);
  }
  close(t) {
    this.match("</") || this.error(`Expected </${t}>`), this.match(`${t}`) || this.error(`Expected </${t}>`), this.whitespace(), this.match(">") || this.error(`Expected </${t}>`);
  }
  children(t) {
    const e = [];
    do {
      this.eof() && this.error(`Expected </${t}>`);
      const s = this.node();
      s !== null && e.push(s);
    } while (!this.peek("</"));
    return e;
  }
  attributes() {
    const t = [];
    for (; !this.peek(">", "/>") && !this.eof(); ) {
      this.whitespace();
      const e = this.line, s = this.identifier("=", ">", "/>");
      s || this.error("Blank attribute name"), this.whitespace();
      let n = "";
      this.match("=") && (this.whitespace(), this.match("'") ? n = this.decodeEntities(this.string("'")) : this.match('"') ? n = this.decodeEntities(this.string('"')) : n = this.decodeEntities(this.identifier(">", "/>"))), this.whitespace(), t.push(new St(s, n, e));
    }
    return t;
  }
  text() {
    const t = this.current, e = this.line;
    let s = 0;
    for (; !this.eof(); ) {
      if (this.match("{{")) {
        s++;
        continue;
      }
      if (s > 0 && this.match("}}")) {
        s--;
        continue;
      }
      if (s === 0 && this.peek("<"))
        break;
      this.advance();
    }
    const n = this.source.slice(t, this.current).trim();
    return n ? new yt(this.decodeEntities(n), e) : null;
  }
  decodeEntities(t) {
    return t.replace(/&nbsp;/g, " ").replace(/&lt;/g, "<").replace(/&gt;/g, ">").replace(/&quot;/g, '"').replace(/&apos;/g, "'").replace(/&amp;/g, "&");
  }
  whitespace() {
    let t = 0;
    for (; this.peek(...J) && !this.eof(); )
      t += 1, this.advance();
    return t;
  }
  identifier(...t) {
    this.whitespace();
    const e = this.current;
    for (; !this.peek(...J, ...t); )
      this.advance(`Expected closing ${t}`);
    const s = this.current;
    return this.whitespace(), this.source.slice(e, s).trim();
  }
  string(t) {
    const e = this.current;
    for (; !this.match(t); )
      this.advance(`Expected closing ${t}`);
    return this.source.slice(e, this.current - 1);
  }
}
let D = null;
const R = [];
let G = !1;
const O = /* @__PURE__ */ new Set(), st = [];
class kt {
  constructor(t) {
    this.subscribers = /* @__PURE__ */ new Set(), this.watchers = /* @__PURE__ */ new Set(), this._value = t;
  }
  get value() {
    return D && (this.subscribers.add(D.fn), D.deps.add(this)), this._value;
  }
  set value(t) {
    if (this._value !== t) {
      const e = this._value;
      if (this._value = t, G) {
        for (const s of this.subscribers) O.add(s);
        for (const s of this.watchers) st.push(() => s(t, e));
      } else {
        for (const s of Array.from(this.subscribers))
          try {
            s();
          } catch (n) {
            console.error("Effect error:", n);
          }
        for (const s of this.watchers)
          try {
            s(t, e);
          } catch (n) {
            console.error("Watcher error:", n);
          }
      }
    }
  }
  onChange(t) {
    return this.watchers.add(t), () => this.watchers.delete(t);
  }
  unsubscribe(t) {
    this.subscribers.delete(t);
  }
  toString() {
    return String(this.value);
  }
  peek() {
    return this._value;
  }
}
function L(i) {
  const t = {
    fn: () => {
      t.deps.forEach((e) => e.unsubscribe(t.fn)), t.deps.clear(), R.push(t), D = t;
      try {
        i();
      } finally {
        R.pop(), D = R[R.length - 1] || null;
      }
    },
    deps: /* @__PURE__ */ new Set()
  };
  return t.fn(), () => {
    t.deps.forEach((e) => e.unsubscribe(t.fn)), t.deps.clear();
  };
}
function it(i) {
  return new kt(i);
}
function Pt(i) {
  G = !0;
  try {
    i();
  } finally {
    G = !1;
    const t = Array.from(O);
    O.clear();
    const e = st.splice(0);
    for (const s of t)
      try {
        s();
      } catch (n) {
        console.error("Effect error:", n);
      }
    for (const s of e)
      try {
        s();
      } catch (n) {
        console.error("Watcher error:", n);
      }
  }
}
function At(i) {
  const t = it(void 0);
  return L(() => {
    t.value = i();
  }), t;
}
class K {
  constructor(t, e = "boundary") {
    this.start = document.createComment(`${e}-start`), this.end = document.createComment(`${e}-end`), t.appendChild(this.start), t.appendChild(this.end);
  }
  clear() {
    var e;
    let t = this.start.nextSibling;
    for (; t && t !== this.end; ) {
      const s = t;
      t = t.nextSibling, (e = s.parentNode) == null || e.removeChild(s);
    }
  }
  insert(t) {
    var e;
    (e = this.end.parentNode) == null || e.insertBefore(t, this.end);
  }
  nodes() {
    const t = [];
    let e = this.start.nextSibling;
    for (; e && e !== this.end; )
      t.push(e), e = e.nextSibling;
    return t;
  }
  get parent() {
    return this.start.parentNode;
  }
}
class Q {
  constructor(t) {
    this.scanner = new et(), this.parser = new T(), this.interpreter = new bt(), this.registry = {}, t && t.registry && (this.registry = t.registry);
  }
  evaluate(t, e) {
    t.accept(this, e);
  }
  bindMethods(t) {
    if (!t || typeof t != "object") return;
    let e = Object.getPrototypeOf(t);
    for (; e && e !== Object.prototype; ) {
      for (const s of Object.getOwnPropertyNames(e))
        typeof t[s] == "function" && s !== "constructor" && !Object.prototype.hasOwnProperty.call(t, s) && (t[s] = t[s].bind(t));
      e = Object.getPrototypeOf(e);
    }
  }
  // Creates an effect that restores the current scope on every re-run,
  // so effects set up inside @each always evaluate in their item scope.
  scopedEffect(t) {
    const e = this.interpreter.scope;
    return L(() => {
      const s = this.interpreter.scope;
      this.interpreter.scope = e;
      try {
        t();
      } finally {
        this.interpreter.scope = s;
      }
    });
  }
  // evaluates expressions and returns the result of the first evaluation
  execute(t, e) {
    const s = this.scanner.scan(t), n = this.parser.parse(s), c = this.interpreter.scope;
    e && (this.interpreter.scope = e);
    const a = n.map(
      (h) => this.interpreter.evaluate(h)
    );
    return this.interpreter.scope = c, a && a.length ? a[0] : void 0;
  }
  transpile(t, e, s) {
    return this.destroy(s), s.innerHTML = "", this.bindMethods(e), this.interpreter.scope.init(e), this.createSiblings(t, s), s;
  }
  visitElementKNode(t, e) {
    this.createElement(t, e);
  }
  visitTextKNode(t, e) {
    try {
      const s = document.createTextNode("");
      e && (e.insert && typeof e.insert == "function" ? e.insert(s) : e.appendChild(s));
      const n = this.scopedEffect(() => {
        s.textContent = this.evaluateTemplateString(t.value);
      });
      this.trackEffect(s, n);
    } catch (s) {
      this.error(s.message || `${s}`, "text node");
    }
  }
  visitAttributeKNode(t, e) {
    const s = document.createAttribute(t.name), n = this.scopedEffect(() => {
      s.value = this.evaluateTemplateString(t.value);
    });
    this.trackEffect(s, n), e && e.setAttributeNode(s);
  }
  visitCommentKNode(t, e) {
    const s = new Comment(t.value);
    e && (e.insert && typeof e.insert == "function" ? e.insert(s) : e.appendChild(s));
  }
  trackEffect(t, e) {
    t.$kasperEffects || (t.$kasperEffects = []), t.$kasperEffects.push(e);
  }
  findAttr(t, e) {
    if (!t || !t.attributes || !t.attributes.length)
      return null;
    const s = t.attributes.find(
      (n) => e.includes(n.name)
    );
    return s || null;
  }
  doIf(t, e) {
    const s = new K(e, "if"), n = this.scopedEffect(() => {
      if (s.nodes().forEach((a) => this.destroyNode(a)), s.clear(), this.execute(t[0][1].value)) {
        this.createElement(t[0][0], s);
        return;
      }
      for (const a of t.slice(1, t.length)) {
        if (this.findAttr(a[0], ["@elseif"]))
          if (this.execute(a[1].value)) {
            this.createElement(a[0], s);
            return;
          } else
            continue;
        if (this.findAttr(a[0], ["@else"])) {
          this.createElement(a[0], s);
          return;
        }
      }
    });
    this.trackEffect(s, n);
  }
  doEach(t, e, s) {
    const n = this.findAttr(e, ["@key"]);
    n ? this.doEachKeyed(t, e, s, n) : this.doEachUnkeyed(t, e, s);
  }
  doEachUnkeyed(t, e, s) {
    const n = new K(s, "each"), c = this.interpreter.scope, a = L(() => {
      n.nodes().forEach((l) => this.destroyNode(l)), n.clear();
      const h = this.scanner.scan(t.value), [d, o, p] = this.interpreter.evaluate(
        this.parser.foreach(h)
      );
      let m = 0;
      for (const l of p) {
        const f = { [d]: l };
        o && (f[o] = m), this.interpreter.scope = new b(c, f), this.createElement(e, n), m += 1;
      }
      this.interpreter.scope = c;
    });
    this.trackEffect(n, a);
  }
  doEachKeyed(t, e, s, n) {
    const c = new K(s, "each"), a = this.interpreter.scope, h = /* @__PURE__ */ new Map(), d = L(() => {
      var g;
      const o = this.scanner.scan(t.value), [p, m, l] = this.interpreter.evaluate(
        this.parser.foreach(o)
      ), f = [];
      let w = 0;
      for (const E of l) {
        const x = { [p]: E };
        m && (x[m] = w), this.interpreter.scope = new b(a, x);
        const y = this.execute(n.value);
        f.push({ item: E, idx: w, key: y }), w++;
      }
      const v = new Set(f.map((E) => E.key));
      for (const [E, x] of h)
        v.has(E) || (this.destroyNode(x), (g = x.parentNode) == null || g.removeChild(x), h.delete(E));
      for (const { item: E, idx: x, key: y } of f) {
        const C = { [p]: E };
        if (m && (C[m] = x), this.interpreter.scope = new b(a, C), h.has(y))
          c.insert(h.get(y));
        else {
          const V = this.createElement(e, c);
          V && h.set(y, V);
        }
      }
      this.interpreter.scope = a;
    });
    this.trackEffect(c, d);
  }
  doWhile(t, e, s) {
    const n = new K(s, "while"), c = this.interpreter.scope, a = this.scopedEffect(() => {
      for (n.nodes().forEach((h) => this.destroyNode(h)), n.clear(), this.interpreter.scope = new b(c); this.execute(t.value); )
        this.createElement(e, n);
      this.interpreter.scope = c;
    });
    this.trackEffect(n, a);
  }
  // executes initialization in the current scope
  doLet(t, e, s) {
    this.execute(t.value);
    const n = this.createElement(e, s);
    this.interpreter.scope.set("$ref", n);
  }
  createSiblings(t, e) {
    let s = 0;
    for (; s < t.length; ) {
      const n = t[s++];
      if (n.type === "element") {
        const c = this.findAttr(n, ["@each"]);
        if (c) {
          this.doEach(c, n, e);
          continue;
        }
        const a = this.findAttr(n, ["@if"]);
        if (a) {
          const o = [[n, a]];
          for (; s < t.length; ) {
            const p = this.findAttr(t[s], [
              "@else",
              "@elseif"
            ]);
            if (p)
              o.push([t[s], p]), s += 1;
            else
              break;
          }
          this.doIf(o, e);
          continue;
        }
        const h = this.findAttr(n, ["@while"]);
        if (h) {
          this.doWhile(h, n, e);
          continue;
        }
        const d = this.findAttr(n, ["@let"]);
        if (d) {
          this.doLet(d, n, e);
          continue;
        }
      }
      this.evaluate(n, e);
    }
  }
  createElement(t, e) {
    var s;
    try {
      if (t.name === "slot") {
        const o = this.findAttr(t, ["name"]), p = o ? o.value : "default", m = this.interpreter.scope.get("$slots");
        m && m[p] && this.createSiblings(m[p], e);
        return;
      }
      const n = t.name === "void", c = !!this.registry[t.name], a = n ? e : document.createElement(t.name), h = this.interpreter.scope;
      if (a && a !== e && this.interpreter.scope.set("$ref", a), c) {
        let o = {};
        const p = t.attributes.filter(
          (f) => f.name.startsWith("@:")
        ), m = this.createComponentArgs(p), l = { default: [] };
        for (const f of t.children) {
          if (f.type === "element") {
            const w = this.findAttr(f, ["slot"]);
            if (w) {
              const v = w.value;
              l[v] || (l[v] = []), l[v].push(f);
              continue;
            }
          }
          l.default.push(f);
        }
        return (s = this.registry[t.name]) != null && s.component && (o = new this.registry[t.name].component({
          args: m,
          ref: a,
          transpiler: this
        }), this.bindMethods(o), a.$kasperInstance = o, typeof o.onInit == "function" && o.onInit()), o.$slots = l, this.interpreter.scope = new b(h, o), this.interpreter.scope.set("$instance", o), this.createSiblings(this.registry[t.name].nodes, a), o && typeof o.onRender == "function" && o.onRender(), this.interpreter.scope = h, e && (e.insert && typeof e.insert == "function" ? e.insert(a) : e.appendChild(a)), a;
      }
      if (!n) {
        const o = t.attributes.filter(
          (l) => l.name.startsWith("@on:")
        );
        for (const l of o)
          this.createEventListener(a, l);
        const p = t.attributes.filter(
          (l) => !l.name.startsWith("@")
        );
        for (const l of p)
          this.evaluate(l, a);
        const m = t.attributes.filter((l) => {
          const f = l.name;
          return f.startsWith("@") && !["@if", "@elseif", "@else", "@each", "@while", "@let", "@key", "@ref"].includes(
            f
          ) && !f.startsWith("@on:") && !f.startsWith("@:");
        });
        for (const l of m) {
          const f = l.name.slice(1);
          if (f === "class") {
            let w = "";
            const v = this.scopedEffect(() => {
              const g = this.execute(l.value), x = (a.getAttribute("class") || "").split(" ").filter((C) => C !== w && C !== "").join(" "), y = x ? `${x} ${g}` : g;
              a.setAttribute("class", y), w = g;
            });
            this.trackEffect(a, v);
          } else {
            const w = this.scopedEffect(() => {
              const v = this.execute(l.value);
              if (v === !1 || v === null || v === void 0)
                f !== "style" && a.removeAttribute(f);
              else if (f === "style") {
                const g = a.getAttribute("style"), E = g && !g.includes(v) ? `${g.endsWith(";") ? g : g + ";"} ${v}` : v;
                a.setAttribute("style", E);
              } else
                a.setAttribute(f, v);
            });
            this.trackEffect(a, w);
          }
        }
      }
      e && !n && (e.insert && typeof e.insert == "function" ? e.insert(a) : e.appendChild(a));
      const d = this.findAttr(t, ["@ref"]);
      if (d && !n) {
        const o = d.value.trim(), p = this.interpreter.scope.get("$instance");
        p ? p[o] = a : this.interpreter.scope.set(o, a);
      }
      return t.self || (this.createSiblings(t.children, a), this.interpreter.scope = h), a;
    } catch (n) {
      this.error(n.message || `${n}`, t.name);
    }
  }
  createComponentArgs(t) {
    if (!t.length)
      return {};
    const e = {};
    for (const s of t) {
      const n = s.name.split(":")[1];
      e[n] = this.execute(s.value);
    }
    return e;
  }
  createEventListener(t, e) {
    const [s, ...n] = e.name.split(":")[1].split("."), c = new b(this.interpreter.scope), a = this.interpreter.scope.get("$instance"), h = {};
    a && a.$abortController && (h.signal = a.$abortController.signal), n.includes("once") && (h.once = !0), n.includes("passive") && (h.passive = !0), n.includes("capture") && (h.capture = !0), t.addEventListener(s, (d) => {
      n.includes("prevent") && d.preventDefault(), n.includes("stop") && d.stopPropagation(), c.set("$event", d), this.execute(e.value, c);
    }, h);
  }
  evaluateTemplateString(t) {
    return t && (/\{\{.+\}\}/ms.test(t) ? t.replace(/\{\{([\s\S]+?)\}\}/g, (s, n) => this.evaluateExpression(n)) : t);
  }
  evaluateExpression(t) {
    const e = this.scanner.scan(t), s = this.parser.parse(e);
    let n = "";
    for (const c of s)
      n += `${this.interpreter.evaluate(c)}`;
    return n;
  }
  destroyNode(t) {
    var e;
    if (t.$kasperInstance) {
      const s = t.$kasperInstance;
      s.onDestroy && s.onDestroy(), s.$abortController && s.$abortController.abort();
    }
    if (t.$kasperEffects && (t.$kasperEffects.forEach((s) => s()), t.$kasperEffects = []), t.attributes)
      for (let s = 0; s < t.attributes.length; s++) {
        const n = t.attributes[s];
        n.$kasperEffects && (n.$kasperEffects.forEach((c) => c()), n.$kasperEffects = []);
      }
    (e = t.childNodes) == null || e.forEach((s) => this.destroyNode(s));
  }
  destroy(t) {
    t.childNodes.forEach((e) => this.destroyNode(e));
  }
  visitDoctypeKNode(t) {
  }
  error(t, e) {
    const s = t.startsWith("Runtime Error") ? t : `Runtime Error: ${t}`;
    throw e && !s.includes(`at <${e}>`) ? new Error(`${s}
  at <${e}>`) : new Error(s);
  }
}
function Ct(i) {
  const t = new I();
  try {
    const e = t.parse(i);
    return JSON.stringify(e);
  } catch (e) {
    return JSON.stringify([e instanceof Error ? e.message : String(e)]);
  }
}
function Nt(i, t, e, s) {
  const c = new I().parse(i);
  return new Q({ registry: s || {} }).transpile(c, t || {}, e);
}
function Dt(i) {
  rt({
    root: "kasper-app",
    entry: "kasper-root",
    registry: {
      "kasper-root": {
        selector: "template",
        component: i,
        template: null,
        nodes: []
      }
    }
  });
}
function Lt(i, t, e) {
  const s = document.createElement(t), n = new e[t].component({
    ref: s,
    transpiler: i,
    args: {}
  });
  return {
    node: s,
    instance: n,
    nodes: e[t].nodes
  };
}
function Bt(i, t) {
  const e = { ...i };
  for (const s of Object.keys(i)) {
    const n = i[s];
    if (n.nodes && n.nodes.length > 0)
      continue;
    if (n.selector) {
      const a = document.querySelector(n.selector);
      if (a) {
        n.template = a, n.nodes = t.parse(a.innerHTML);
        continue;
      }
    }
    const c = n.component.template;
    c && (n.nodes = t.parse(c));
  }
  return e;
}
function rt(i) {
  const t = new I(), e = typeof i.root == "string" ? document.querySelector(i.root) : i.root;
  if (!e)
    throw new Error(`Root element not found: ${i.root}`);
  const s = Bt(i.registry, t), n = new Q({ registry: s }), c = i.entry || "kasper-app", { node: a, instance: h, nodes: d } = Lt(
    n,
    c,
    s
  );
  return e && (e.innerHTML = "", e.appendChild(a)), typeof h.onInit == "function" && h.onInit(), n.transpile(d, h, a), typeof h.onRender == "function" && h.onRender(), h;
}
class Rt {
  constructor() {
    this.errors = [];
  }
  evaluate(t) {
    return t.accept(this);
  }
  transpile(t) {
    this.errors = [];
    const e = [];
    for (const s of t)
      try {
        e.push(this.evaluate(s));
      } catch (n) {
        if (console.error(`${n}`), this.errors.push(`${n}`), this.errors.length > 100)
          return this.errors.push("Error limit exceeded"), e;
      }
    return e;
  }
  visitElementKNode(t) {
    let e = t.attributes.map((n) => this.evaluate(n)).join(" ");
    if (e.length && (e = " " + e), t.self)
      return `<${t.name}${e}/>`;
    const s = t.children.map((n) => this.evaluate(n)).join("");
    return `<${t.name}${e}>${s}</${t.name}>`;
  }
  visitAttributeKNode(t) {
    return t.value ? `${t.name}="${t.value}"` : t.name;
  }
  visitTextKNode(t) {
    return t.value.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/\u00a0/g, "&nbsp;");
  }
  visitCommentKNode(t) {
    return `<!-- ${t.value} -->`;
  }
  visitDoctypeKNode(t) {
    return `<!doctype ${t.value}>`;
  }
  error(t) {
    throw new Error(`Runtime Error => ${t}`);
  }
}
typeof window < "u" && ((window || {}).kasper = {
  execute: Ct,
  transpile: Nt,
  App: rt,
  Component: U,
  TemplateParser: I,
  Transpiler: Q,
  Viewer: Rt,
  signal: it,
  effect: L,
  computed: At,
  batch: Pt
}, window.Kasper = Dt, window.Component = U);
export {
  rt as App,
  U as Component,
  T as ExpressionParser,
  bt as Interpreter,
  Dt as Kasper,
  et as Scanner,
  I as TemplateParser,
  Q as Transpiler,
  Rt as Viewer,
  Pt as batch,
  At as computed,
  L as effect,
  Ct as execute,
  it as signal,
  Nt as transpile
};
