const E = {
  // Bootstrap
  ROOT_ELEMENT_NOT_FOUND: "K001-1",
  ENTRY_COMPONENT_NOT_FOUND: "K001-2",
  // Scanner
  UNTERMINATED_COMMENT: "K002-1",
  UNTERMINATED_STRING: "K002-2",
  UNEXPECTED_CHARACTER: "K002-3",
  // Template Parser
  UNEXPECTED_EOF: "K003-1",
  UNEXPECTED_CLOSING_TAG: "K003-2",
  EXPECTED_TAG_NAME: "K003-3",
  EXPECTED_CLOSING_BRACKET: "K003-4",
  EXPECTED_CLOSING_TAG: "K003-5",
  BLANK_ATTRIBUTE_NAME: "K003-6",
  MISPLACED_CONDITIONAL: "K003-7",
  DUPLICATE_IF: "K003-8",
  MULTIPLE_STRUCTURAL_DIRECTIVES: "K003-9",
  // Expression Parser
  UNEXPECTED_TOKEN: "K004-1",
  INVALID_LVALUE: "K004-2",
  EXPECTED_EXPRESSION: "K004-3",
  INVALID_DICTIONARY_KEY: "K004-4",
  // Interpreter
  INVALID_POSTFIX_LVALUE: "K005-1",
  UNKNOWN_BINARY_OPERATOR: "K005-2",
  INVALID_PREFIX_RVALUE: "K005-3",
  UNKNOWN_UNARY_OPERATOR: "K005-4",
  NOT_A_FUNCTION: "K005-5",
  NOT_A_CLASS: "K005-6",
  // Signals
  CIRCULAR_COMPUTED: "K006-1",
  // Transpiler
  RUNTIME_ERROR: "K007-1",
  MISSING_REQUIRED_ATTR: "K007-2"
}, xt = {
  "K001-1": (i) => `Root element not found: ${i.root}`,
  "K001-2": (i) => `Entry component <${i.tag}> not found in registry.`,
  "K002-1": () => 'Unterminated comment, expecting closing "*/"',
  "K002-2": (i) => `Unterminated string, expecting closing ${i.quote}`,
  "K002-3": (i) => `Unexpected character '${i.char}'`,
  "K003-1": (i) => `Unexpected end of file. ${i.eofError}`,
  "K003-2": () => "Unexpected closing tag",
  "K003-3": () => "Expected a tag name",
  "K003-4": () => "Expected closing tag >",
  "K003-5": (i) => `Expected </${i.name}>`,
  "K003-6": () => "Blank attribute name",
  "K003-7": (i) => `@${i.name} must be preceded by an @if or @elseif block.`,
  "K003-8": () => "Multiple conditional directives (@if, @elseif, @else) on the same element are not allowed.",
  "K003-9": () => "Multiple structural directives (@if, @each) on the same element are not allowed. Nest them or use <void> instead.",
  "K004-1": (i) => `${i.message}, unexpected token "${i.token}"`,
  "K004-2": () => "Invalid l-value, is not an assigning target.",
  "K004-3": (i) => `Expected expression, unexpected token "${i.token}"`,
  "K004-4": (i) => `String, Number or Identifier expected as a Key of Dictionary {, unexpected token ${i.token}`,
  "K005-1": (i) => `Invalid left-hand side in postfix operation: ${i.entity}`,
  "K005-2": (i) => `Unknown binary operator ${i.operator}`,
  "K005-3": (i) => `Invalid right-hand side expression in prefix operation: ${i.right}`,
  "K005-4": (i) => `Unknown unary operator ${i.operator}`,
  "K005-5": (i) => `${i.callee} is not a function`,
  "K005-6": (i) => `'${i.clazz}' is not a class. 'new' statement must be used with classes.`,
  "K006-1": () => "Circular dependency detected in computed signal",
  "K007-1": (i) => i.message,
  "K007-2": (i) => i.message
};
class C extends Error {
  constructor(t, e = {}, s, n, a) {
    const o = typeof process < "u" ? process.env.NODE_ENV !== "production" : !1, c = xt[t], m = c ? c(e) : typeof e == "string" ? e : "Unknown error", d = s !== void 0 ? ` (${s}:${n})` : "", l = a ? `
  at <${a}>` : "", p = o ? `

See: https://kasperjs.top/reference/errors#${t.toLowerCase().replace(".", "")}` : "";
    super(`[${t}] ${m}${d}${l}${p}`), this.code = t, this.args = e, this.line = s, this.col = n, this.tagName = a, this.name = "KasperError";
  }
  withTag(t) {
    return this.tagName || (this.tagName = t, this.message += `
  at <${t}>`), this;
  }
}
let U = null;
const V = [];
let Y = !1;
const H = /* @__PURE__ */ new Set(), mt = [];
class Et {
  constructor(t) {
    this.subscribers = /* @__PURE__ */ new Set(), this.watchers = /* @__PURE__ */ new Set(), this._value = t;
  }
  get value() {
    return U && (this.subscribers.add(U.fn), U.deps.add(this)), this._value;
  }
  set value(t) {
    if (this._value !== t) {
      const e = this._value;
      if (this._value = t, Y) {
        for (const s of this.subscribers) H.add(s);
        for (const s of this.watchers) mt.push(() => s(t, e));
      } else {
        const s = Array.from(this.subscribers);
        for (const n of s)
          n();
        for (const n of this.watchers)
          try {
            n(t, e);
          } catch (a) {
            console.error("Watcher error:", a);
          }
      }
    }
  }
  onChange(t, e) {
    var n;
    if ((n = e == null ? void 0 : e.signal) != null && n.aborted) return () => {
    };
    this.watchers.add(t);
    const s = () => this.watchers.delete(t);
    return e != null && e.signal && e.signal.addEventListener("abort", s, { once: !0 }), s;
  }
  unsubscribe(t) {
    this.subscribers.delete(t);
  }
  toString() {
    return String(this.value);
  }
  peek() {
    return this._value;
  }
}
class St extends Et {
  constructor(t, e) {
    super(void 0), this.computing = !1, this.fn = t;
    const s = tt(() => {
      if (this.computing)
        throw new C(E.CIRCULAR_COMPUTED);
      this.computing = !0;
      try {
        super.value = this.fn();
      } finally {
        this.computing = !1;
      }
    }, e);
    e != null && e.signal && e.signal.addEventListener("abort", s, { once: !0 });
  }
  get value() {
    return super.value;
  }
  set value(t) {
  }
}
function tt(i, t) {
  var n;
  if ((n = t == null ? void 0 : t.signal) != null && n.aborted) return () => {
  };
  const e = {
    fn: () => {
      e.deps.forEach((a) => a.unsubscribe(e.fn)), e.deps.clear(), V.push(e), U = e;
      try {
        i();
      } finally {
        V.pop(), U = V[V.length - 1] || null;
      }
    },
    deps: /* @__PURE__ */ new Set()
  };
  e.fn();
  const s = () => {
    e.deps.forEach((a) => a.unsubscribe(e.fn)), e.deps.clear();
  };
  return s.run = e.fn, t != null && t.signal && t.signal.addEventListener("abort", s, { once: !0 }), s;
}
function Ft(i) {
  return new Et(i);
}
function zt(i, t, e) {
  return i.onChange(t, e);
}
function jt(i) {
  Y = !0;
  try {
    i();
  } finally {
    Y = !1;
    const t = Array.from(H);
    H.clear();
    const e = mt.splice(0);
    for (const s of t)
      s();
    for (const s of e)
      try {
        s();
      } catch (n) {
        console.error("Watcher error:", n);
      }
  }
}
function Nt(i, t) {
  return new St(i, t);
}
class At {
  constructor(t) {
    if (this.args = {}, this.$abortController = new AbortController(), !t) {
      this.args = {};
      return;
    }
    t.args && (this.args = t.args), t.ref && (this.ref = t.ref), t.transpiler && (this.transpiler = t.transpiler);
  }
  /**
   * Creates a reactive effect tied to the component's lifecycle.
   * Runs immediately and re-runs when any signal dependency changes.
   */
  effect(t) {
    tt(t, { signal: this.$abortController.signal });
  }
  /**
   * Watches a specific signal for changes.
   * Does NOT run immediately.
   */
  watch(t, e) {
    t.onChange(e, { signal: this.$abortController.signal });
  }
  /**
   * Creates a computed signal tied to the component's lifecycle.
   * The internal effect is automatically cleaned up when the component is destroyed.
   */
  computed(t) {
    return Nt(t, { signal: this.$abortController.signal });
  }
  onMount() {
  }
  onRender() {
  }
  onChanges() {
  }
  onDestroy() {
  }
  render() {
    var t;
    (t = this.$render) == null || t.call(this);
  }
}
class v {
  // tslint:disable-next-line
  constructor() {
  }
}
class rt extends v {
  constructor(t, e, s) {
    super(), this.params = t, this.body = e, this.line = s;
  }
  accept(t) {
    return t.visitArrowFunctionExpr(this);
  }
  toString() {
    return "Expr.ArrowFunction";
  }
}
class Ct extends v {
  constructor(t, e, s) {
    super(), this.name = t, this.value = e, this.line = s;
  }
  accept(t) {
    return t.visitAssignExpr(this);
  }
  toString() {
    return "Expr.Assign";
  }
}
class D extends v {
  constructor(t, e, s, n) {
    super(), this.left = t, this.operator = e, this.right = s, this.line = n;
  }
  accept(t) {
    return t.visitBinaryExpr(this);
  }
  toString() {
    return "Expr.Binary";
  }
}
class J extends v {
  constructor(t, e, s, n, a = !1) {
    super(), this.callee = t, this.paren = e, this.args = s, this.line = n, this.optional = a;
  }
  accept(t) {
    return t.visitCallExpr(this);
  }
  toString() {
    return "Expr.Call";
  }
}
class yt extends v {
  constructor(t, e) {
    super(), this.value = t, this.line = e;
  }
  accept(t) {
    return t.visitDebugExpr(this);
  }
  toString() {
    return "Expr.Debug";
  }
}
class nt extends v {
  constructor(t, e) {
    super(), this.properties = t, this.line = e;
  }
  accept(t) {
    return t.visitDictionaryExpr(this);
  }
  toString() {
    return "Expr.Dictionary";
  }
}
class Rt extends v {
  constructor(t, e, s, n) {
    super(), this.name = t, this.key = e, this.iterable = s, this.line = n;
  }
  accept(t) {
    return t.visitEachExpr(this);
  }
  toString() {
    return "Expr.Each";
  }
}
class R extends v {
  constructor(t, e, s, n) {
    super(), this.entity = t, this.key = e, this.type = s, this.line = n;
  }
  accept(t) {
    return t.visitGetExpr(this);
  }
  toString() {
    return "Expr.Get";
  }
}
class Pt extends v {
  constructor(t, e) {
    super(), this.expression = t, this.line = e;
  }
  accept(t) {
    return t.visitGroupingExpr(this);
  }
  toString() {
    return "Expr.Grouping";
  }
}
class z extends v {
  constructor(t, e) {
    super(), this.name = t, this.line = e;
  }
  accept(t) {
    return t.visitKeyExpr(this);
  }
  toString() {
    return "Expr.Key";
  }
}
class at extends v {
  constructor(t, e, s, n) {
    super(), this.left = t, this.operator = e, this.right = s, this.line = n;
  }
  accept(t) {
    return t.visitLogicalExpr(this);
  }
  toString() {
    return "Expr.Logical";
  }
}
class ct extends v {
  constructor(t, e) {
    super(), this.value = t, this.line = e;
  }
  accept(t) {
    return t.visitListExpr(this);
  }
  toString() {
    return "Expr.List";
  }
}
class _ extends v {
  constructor(t, e) {
    super(), this.value = t, this.line = e;
  }
  accept(t) {
    return t.visitLiteralExpr(this);
  }
  toString() {
    return "Expr.Literal";
  }
}
class ot extends v {
  constructor(t, e, s) {
    super(), this.clazz = t, this.args = e, this.line = s;
  }
  accept(t) {
    return t.visitNewExpr(this);
  }
  toString() {
    return "Expr.New";
  }
}
class kt extends v {
  constructor(t, e, s) {
    super(), this.left = t, this.right = e, this.line = s;
  }
  accept(t) {
    return t.visitNullCoalescingExpr(this);
  }
  toString() {
    return "Expr.NullCoalescing";
  }
}
class ht extends v {
  constructor(t, e, s) {
    super(), this.entity = t, this.increment = e, this.line = s;
  }
  accept(t) {
    return t.visitPostfixExpr(this);
  }
  toString() {
    return "Expr.Postfix";
  }
}
let B = class extends v {
  constructor(t, e, s, n) {
    super(), this.entity = t, this.key = e, this.value = s, this.line = n;
  }
  accept(t) {
    return t.visitSetExpr(this);
  }
  toString() {
    return "Expr.Set";
  }
};
class Dt extends v {
  constructor(t, e, s) {
    super(), this.left = t, this.right = e, this.line = s;
  }
  accept(t) {
    return t.visitPipelineExpr(this);
  }
  toString() {
    return "Expr.Pipeline";
  }
}
class I extends v {
  constructor(t, e) {
    super(), this.value = t, this.line = e;
  }
  accept(t) {
    return t.visitSpreadExpr(this);
  }
  toString() {
    return "Expr.Spread";
  }
}
class _t extends v {
  constructor(t, e) {
    super(), this.value = t, this.line = e;
  }
  accept(t) {
    return t.visitTemplateExpr(this);
  }
  toString() {
    return "Expr.Template";
  }
}
class It extends v {
  constructor(t, e, s, n) {
    super(), this.condition = t, this.thenExpr = e, this.elseExpr = s, this.line = n;
  }
  accept(t) {
    return t.visitTernaryExpr(this);
  }
  toString() {
    return "Expr.Ternary";
  }
}
class Lt extends v {
  constructor(t, e) {
    super(), this.value = t, this.line = e;
  }
  accept(t) {
    return t.visitTypeofExpr(this);
  }
  toString() {
    return "Expr.Typeof";
  }
}
class Kt extends v {
  constructor(t, e, s) {
    super(), this.operator = t, this.right = e, this.line = s;
  }
  accept(t) {
    return t.visitUnaryExpr(this);
  }
  toString() {
    return "Expr.Unary";
  }
}
class O extends v {
  constructor(t, e) {
    super(), this.name = t, this.line = e;
  }
  accept(t) {
    return t.visitVariableExpr(this);
  }
  toString() {
    return "Expr.Variable";
  }
}
class $t extends v {
  constructor(t, e) {
    super(), this.value = t, this.line = e;
  }
  accept(t) {
    return t.visitVoidExpr(this);
  }
  toString() {
    return "Expr.Void";
  }
}
var r = /* @__PURE__ */ ((i) => (i[i.Eof = 0] = "Eof", i[i.Panic = 1] = "Panic", i[i.Ampersand = 2] = "Ampersand", i[i.AtSign = 3] = "AtSign", i[i.Caret = 4] = "Caret", i[i.Comma = 5] = "Comma", i[i.Dollar = 6] = "Dollar", i[i.Dot = 7] = "Dot", i[i.Hash = 8] = "Hash", i[i.LeftBrace = 9] = "LeftBrace", i[i.LeftBracket = 10] = "LeftBracket", i[i.LeftParen = 11] = "LeftParen", i[i.Percent = 12] = "Percent", i[i.Pipe = 13] = "Pipe", i[i.RightBrace = 14] = "RightBrace", i[i.RightBracket = 15] = "RightBracket", i[i.RightParen = 16] = "RightParen", i[i.Semicolon = 17] = "Semicolon", i[i.Slash = 18] = "Slash", i[i.Star = 19] = "Star", i[i.Arrow = 20] = "Arrow", i[i.Bang = 21] = "Bang", i[i.BangEqual = 22] = "BangEqual", i[i.BangEqualEqual = 23] = "BangEqualEqual", i[i.Colon = 24] = "Colon", i[i.Equal = 25] = "Equal", i[i.EqualEqual = 26] = "EqualEqual", i[i.EqualEqualEqual = 27] = "EqualEqualEqual", i[i.Greater = 28] = "Greater", i[i.GreaterEqual = 29] = "GreaterEqual", i[i.Less = 30] = "Less", i[i.LessEqual = 31] = "LessEqual", i[i.Minus = 32] = "Minus", i[i.MinusEqual = 33] = "MinusEqual", i[i.MinusMinus = 34] = "MinusMinus", i[i.PercentEqual = 35] = "PercentEqual", i[i.Plus = 36] = "Plus", i[i.PlusEqual = 37] = "PlusEqual", i[i.PlusPlus = 38] = "PlusPlus", i[i.Question = 39] = "Question", i[i.QuestionDot = 40] = "QuestionDot", i[i.QuestionQuestion = 41] = "QuestionQuestion", i[i.SlashEqual = 42] = "SlashEqual", i[i.StarEqual = 43] = "StarEqual", i[i.DotDot = 44] = "DotDot", i[i.DotDotDot = 45] = "DotDotDot", i[i.LessEqualGreater = 46] = "LessEqualGreater", i[i.Identifier = 47] = "Identifier", i[i.Template = 48] = "Template", i[i.String = 49] = "String", i[i.Number = 50] = "Number", i[i.LeftShift = 51] = "LeftShift", i[i.RightShift = 52] = "RightShift", i[i.Pipeline = 53] = "Pipeline", i[i.Tilde = 54] = "Tilde", i[i.And = 55] = "And", i[i.Const = 56] = "Const", i[i.Debug = 57] = "Debug", i[i.False = 58] = "False", i[i.In = 59] = "In", i[i.Instanceof = 60] = "Instanceof", i[i.New = 61] = "New", i[i.Null = 62] = "Null", i[i.Undefined = 63] = "Undefined", i[i.Of = 64] = "Of", i[i.Or = 65] = "Or", i[i.True = 66] = "True", i[i.Typeof = 67] = "Typeof", i[i.Void = 68] = "Void", i[i.With = 69] = "With", i))(r || {});
class lt {
  constructor(t, e, s, n, a) {
    this.name = r[t], this.type = t, this.lexeme = e, this.literal = s, this.line = n, this.col = a;
  }
  toString() {
    return `[(${this.line}):"${this.lexeme}"]`;
  }
}
const ut = [" ", `
`, "	", "\r"], qt = [
  "area",
  "base",
  "br",
  "col",
  "embed",
  "hr",
  "img",
  "input",
  "link",
  "meta",
  "param",
  "source",
  "track",
  "wbr"
];
class gt {
  parse(t) {
    this.current = 0, this.tokens = t;
    const e = [];
    for (; !this.eof(); )
      e.push(this.expression());
    return e;
  }
  match(...t) {
    for (const e of t)
      if (this.check(e))
        return this.advance(), !0;
    return !1;
  }
  advance() {
    return this.eof() || this.current++, this.previous();
  }
  peek() {
    return this.tokens[this.current];
  }
  previous() {
    return this.tokens[this.current - 1];
  }
  check(t) {
    return this.peek().type === t;
  }
  eof() {
    return this.check(r.Eof);
  }
  consume(t, e) {
    return this.check(t) ? this.advance() : this.error(
      E.UNEXPECTED_TOKEN,
      this.peek(),
      { message: e, token: this.peek().lexeme }
    );
  }
  error(t, e, s = {}) {
    throw new C(t, s, e.line, e.col);
  }
  synchronize() {
    do {
      if (this.check(r.Semicolon) || this.check(r.RightBrace)) {
        this.advance();
        return;
      }
      this.advance();
    } while (!this.eof());
  }
  foreach(t) {
    this.current = 0, this.tokens = t;
    const e = this.consume(
      r.Identifier,
      'Expected an identifier inside "each" statement'
    );
    let s = null;
    this.match(r.With) && (s = this.consume(
      r.Identifier,
      'Expected a "key" identifier after "with" keyword in foreach statement'
    )), this.consume(
      r.Of,
      'Expected "of" keyword inside foreach statement'
    );
    const n = this.expression();
    return new Rt(e, s, n, e.line);
  }
  expression() {
    const t = this.assignment();
    if (this.match(r.Semicolon))
      for (; this.match(r.Semicolon); )
        ;
    return t;
  }
  assignment() {
    const t = this.pipeline();
    if (this.match(
      r.Equal,
      r.PlusEqual,
      r.MinusEqual,
      r.StarEqual,
      r.SlashEqual
    )) {
      const e = this.previous();
      let s = this.assignment();
      if (t instanceof O) {
        const n = t.name;
        return e.type !== r.Equal && (s = new D(
          new O(n, n.line),
          e,
          s,
          e.line
        )), new Ct(n, s, n.line);
      } else if (t instanceof R)
        return e.type !== r.Equal && (s = new D(
          new R(t.entity, t.key, t.type, t.line),
          e,
          s,
          e.line
        )), new B(t.entity, t.key, s, t.line);
      this.error(E.INVALID_LVALUE, e);
    }
    return t;
  }
  pipeline() {
    let t = this.ternary();
    for (; this.match(r.Pipeline); ) {
      const e = this.ternary();
      t = new Dt(t, e, t.line);
    }
    return t;
  }
  ternary() {
    const t = this.nullCoalescing();
    if (this.match(r.Question)) {
      const e = this.ternary();
      this.consume(r.Colon, 'Expected ":" after ternary ? expression');
      const s = this.ternary();
      return new It(t, e, s, t.line);
    }
    return t;
  }
  nullCoalescing() {
    const t = this.logicalOr();
    if (this.match(r.QuestionQuestion)) {
      const e = this.nullCoalescing();
      return new kt(t, e, t.line);
    }
    return t;
  }
  logicalOr() {
    let t = this.logicalAnd();
    for (; this.match(r.Or); ) {
      const e = this.previous(), s = this.logicalAnd();
      t = new at(t, e, s, e.line);
    }
    return t;
  }
  logicalAnd() {
    let t = this.equality();
    for (; this.match(r.And); ) {
      const e = this.previous(), s = this.equality();
      t = new at(t, e, s, e.line);
    }
    return t;
  }
  equality() {
    let t = this.shift();
    for (; this.match(
      r.BangEqual,
      r.BangEqualEqual,
      r.EqualEqual,
      r.EqualEqualEqual,
      r.Greater,
      r.GreaterEqual,
      r.Less,
      r.LessEqual,
      r.Instanceof,
      r.In
    ); ) {
      const e = this.previous(), s = this.shift();
      t = new D(t, e, s, e.line);
    }
    return t;
  }
  shift() {
    let t = this.addition();
    for (; this.match(r.LeftShift, r.RightShift); ) {
      const e = this.previous(), s = this.addition();
      t = new D(t, e, s, e.line);
    }
    return t;
  }
  addition() {
    let t = this.modulus();
    for (; this.match(r.Minus, r.Plus); ) {
      const e = this.previous(), s = this.modulus();
      t = new D(t, e, s, e.line);
    }
    return t;
  }
  modulus() {
    let t = this.multiplication();
    for (; this.match(r.Percent); ) {
      const e = this.previous(), s = this.multiplication();
      t = new D(t, e, s, e.line);
    }
    return t;
  }
  multiplication() {
    let t = this.typeof();
    for (; this.match(r.Slash, r.Star); ) {
      const e = this.previous(), s = this.typeof();
      t = new D(t, e, s, e.line);
    }
    return t;
  }
  typeof() {
    if (this.match(r.Typeof)) {
      const t = this.previous(), e = this.typeof();
      return new Lt(e, t.line);
    }
    return this.unary();
  }
  unary() {
    if (this.match(
      r.Minus,
      r.Bang,
      r.Tilde,
      r.Dollar,
      r.PlusPlus,
      r.MinusMinus
    )) {
      const t = this.previous(), e = this.unary();
      return new Kt(t, e, t.line);
    }
    return this.newKeyword();
  }
  newKeyword() {
    if (this.match(r.New)) {
      const t = this.previous(), e = this.call();
      return e instanceof J ? new ot(e.callee, e.args, t.line) : new ot(e, [], t.line);
    }
    return this.postfix();
  }
  postfix() {
    const t = this.call();
    return this.match(r.PlusPlus) ? new ht(t, 1, t.line) : this.match(r.MinusMinus) ? new ht(t, -1, t.line) : t;
  }
  call() {
    let t = this.primary(), e;
    do {
      if (e = !1, this.match(r.LeftParen)) {
        e = !0;
        do
          t = this.finishCall(t, this.previous(), !1);
        while (this.match(r.LeftParen));
      }
      if (this.match(r.Dot, r.QuestionDot)) {
        e = !0;
        const s = this.previous();
        s.type === r.QuestionDot && this.match(r.LeftBracket) ? t = this.bracketGet(t, s) : s.type === r.QuestionDot && this.match(r.LeftParen) ? t = this.finishCall(t, this.previous(), !0) : t = this.dotGet(t, s);
      }
      this.match(r.LeftBracket) && (e = !0, t = this.bracketGet(t, this.previous()));
    } while (e);
    return t;
  }
  tokenAt(t) {
    var e;
    return (e = this.tokens[this.current + t]) == null ? void 0 : e.type;
  }
  isArrowParams() {
    var e, s, n, a, o, c;
    let t = this.current + 1;
    if (((e = this.tokens[t]) == null ? void 0 : e.type) === r.RightParen)
      return ((s = this.tokens[t + 1]) == null ? void 0 : s.type) === r.Arrow;
    for (; t < this.tokens.length; ) {
      if (((n = this.tokens[t]) == null ? void 0 : n.type) !== r.Identifier) return !1;
      if (t++, ((a = this.tokens[t]) == null ? void 0 : a.type) === r.RightParen)
        return ((o = this.tokens[t + 1]) == null ? void 0 : o.type) === r.Arrow;
      if (((c = this.tokens[t]) == null ? void 0 : c.type) !== r.Comma) return !1;
      t++;
    }
    return !1;
  }
  finishCall(t, e, s) {
    const n = [];
    if (!this.check(r.RightParen))
      do
        this.match(r.DotDotDot) ? n.push(new I(this.expression(), this.previous().line)) : n.push(this.expression());
      while (this.match(r.Comma));
    const a = this.consume(r.RightParen, 'Expected ")" after arguments');
    return new J(t, a, n, a.line, s);
  }
  dotGet(t, e) {
    const s = this.consume(
      r.Identifier,
      "Expect property name after '.'"
    ), n = new z(s, s.line);
    return new R(t, n, e.type, s.line);
  }
  bracketGet(t, e) {
    let s = null;
    return this.check(r.RightBracket) || (s = this.expression()), this.consume(r.RightBracket, 'Expected "]" after an index'), new R(t, s, e.type, e.line);
  }
  primary() {
    if (this.match(r.False))
      return new _(!1, this.previous().line);
    if (this.match(r.True))
      return new _(!0, this.previous().line);
    if (this.match(r.Null))
      return new _(null, this.previous().line);
    if (this.match(r.Undefined))
      return new _(void 0, this.previous().line);
    if (this.match(r.Number) || this.match(r.String))
      return new _(this.previous().literal, this.previous().line);
    if (this.match(r.Template))
      return new _t(this.previous().literal, this.previous().line);
    if (this.check(r.Identifier) && this.tokenAt(1) === r.Arrow) {
      const t = this.advance();
      this.advance();
      const e = this.expression();
      return new rt([t], e, t.line);
    }
    if (this.match(r.Identifier)) {
      const t = this.previous();
      return new O(t, t.line);
    }
    if (this.check(r.LeftParen) && this.isArrowParams()) {
      this.advance();
      const t = [];
      if (!this.check(r.RightParen))
        do
          t.push(this.consume(r.Identifier, "Expected parameter name"));
        while (this.match(r.Comma));
      this.consume(r.RightParen, 'Expected ")"'), this.consume(r.Arrow, 'Expected "=>"');
      const e = this.expression();
      return new rt(t, e, this.previous().line);
    }
    if (this.match(r.LeftParen)) {
      const t = this.expression();
      return this.consume(r.RightParen, 'Expected ")" after expression'), new Pt(t, t.line);
    }
    if (this.match(r.LeftBrace))
      return this.dictionary();
    if (this.match(r.LeftBracket))
      return this.list();
    if (this.match(r.Void)) {
      const t = this.expression();
      return new $t(t, this.previous().line);
    }
    if (this.match(r.Debug)) {
      const t = this.expression();
      return new yt(t, this.previous().line);
    }
    throw this.error(
      E.EXPECTED_EXPRESSION,
      this.peek(),
      { token: this.peek().lexeme }
    );
  }
  dictionary() {
    const t = this.previous();
    if (this.match(r.RightBrace))
      return new nt([], this.previous().line);
    const e = [];
    do
      if (this.match(r.DotDotDot))
        e.push(new I(this.expression(), this.previous().line));
      else if (this.match(r.String, r.Identifier, r.Number)) {
        const s = this.previous();
        if (this.match(r.Colon)) {
          const n = this.expression();
          e.push(
            new B(null, new z(s, s.line), n, s.line)
          );
        } else {
          const n = new O(s, s.line);
          e.push(
            new B(null, new z(s, s.line), n, s.line)
          );
        }
      } else
        this.error(
          E.INVALID_DICTIONARY_KEY,
          this.peek(),
          { token: this.peek().lexeme }
        );
    while (this.match(r.Comma));
    return this.consume(r.RightBrace, 'Expected "}" after object literal'), new nt(e, t.line);
  }
  list() {
    const t = [], e = this.previous();
    if (this.match(r.RightBracket))
      return new ct([], this.previous().line);
    do
      this.match(r.DotDotDot) ? t.push(new I(this.expression(), this.previous().line)) : t.push(this.expression());
    while (this.match(r.Comma));
    return this.consume(
      r.RightBracket,
      'Expected "]" after array declaration'
    ), new ct(t, e.line);
  }
}
function q(i) {
  return i >= "0" && i <= "9";
}
function vt(i) {
  return i >= "a" && i <= "z" || i >= "A" && i <= "Z" || i === "$" || i === "_";
}
function Ot(i) {
  return vt(i) || q(i);
}
function Mt(i) {
  return i.charAt(0).toUpperCase() + i.substring(1).toLowerCase();
}
function Ut(i) {
  return r[i] >= r.And;
}
class wt {
  scan(t) {
    for (this.source = t, this.tokens = [], this.current = 0, this.start = 0, this.line = 1, this.col = 1; !this.eof(); )
      this.start = this.current, this.getToken();
    return this.tokens.push(new lt(r.Eof, "", null, this.line, 0)), this.tokens;
  }
  eof() {
    return this.current >= this.source.length;
  }
  advance() {
    return this.peek() === `
` && (this.line++, this.col = 0), this.current++, this.col++, this.source.charAt(this.current - 1);
  }
  addToken(t, e) {
    const s = this.source.substring(this.start, this.current);
    this.tokens.push(new lt(t, s, e, this.line, this.col));
  }
  match(t) {
    return this.eof() || this.source.charAt(this.current) !== t ? !1 : (this.current++, !0);
  }
  peek() {
    return this.eof() ? "\0" : this.source.charAt(this.current);
  }
  peekNext() {
    return this.current + 1 >= this.source.length ? "\0" : this.source.charAt(this.current + 1);
  }
  comment() {
    for (; this.peek() !== `
` && !this.eof(); )
      this.advance();
  }
  multilineComment() {
    for (; !this.eof() && !(this.peek() === "*" && this.peekNext() === "/"); )
      this.advance();
    this.eof() ? this.error(E.UNTERMINATED_COMMENT) : (this.advance(), this.advance());
  }
  string(t) {
    for (; this.peek() !== t && !this.eof(); )
      this.advance();
    if (this.eof()) {
      this.error(E.UNTERMINATED_STRING, { quote: t });
      return;
    }
    this.advance();
    const e = this.source.substring(this.start + 1, this.current - 1);
    this.addToken(t !== "`" ? r.String : r.Template, e);
  }
  number() {
    for (; q(this.peek()); )
      this.advance();
    for (this.peek() === "." && q(this.peekNext()) && this.advance(); q(this.peek()); )
      this.advance();
    for (this.peek().toLowerCase() === "e" && (this.advance(), (this.peek() === "-" || this.peek() === "+") && this.advance()); q(this.peek()); )
      this.advance();
    const t = this.source.substring(this.start, this.current);
    this.addToken(r.Number, Number(t));
  }
  identifier() {
    for (; Ot(this.peek()); )
      this.advance();
    const t = this.source.substring(this.start, this.current), e = Mt(t);
    Ut(e) ? this.addToken(r[e], t) : this.addToken(r.Identifier, t);
  }
  getToken() {
    const t = this.advance();
    switch (t) {
      case "(":
        this.addToken(r.LeftParen, null);
        break;
      case ")":
        this.addToken(r.RightParen, null);
        break;
      case "[":
        this.addToken(r.LeftBracket, null);
        break;
      case "]":
        this.addToken(r.RightBracket, null);
        break;
      case "{":
        this.addToken(r.LeftBrace, null);
        break;
      case "}":
        this.addToken(r.RightBrace, null);
        break;
      case ",":
        this.addToken(r.Comma, null);
        break;
      case ";":
        this.addToken(r.Semicolon, null);
        break;
      case "~":
        this.addToken(r.Tilde, null);
        break;
      case "^":
        this.addToken(r.Caret, null);
        break;
      case "#":
        this.addToken(r.Hash, null);
        break;
      case ":":
        this.addToken(
          this.match("=") ? r.Arrow : r.Colon,
          null
        );
        break;
      case "*":
        this.addToken(
          this.match("=") ? r.StarEqual : r.Star,
          null
        );
        break;
      case "%":
        this.addToken(
          this.match("=") ? r.PercentEqual : r.Percent,
          null
        );
        break;
      case "|":
        this.addToken(
          this.match("|") ? r.Or : this.match(">") ? r.Pipeline : r.Pipe,
          null
        );
        break;
      case "&":
        this.addToken(
          this.match("&") ? r.And : r.Ampersand,
          null
        );
        break;
      case ">":
        this.addToken(
          this.match(">") ? r.RightShift : this.match("=") ? r.GreaterEqual : r.Greater,
          null
        );
        break;
      case "!":
        this.addToken(
          this.match("=") ? this.match("=") ? r.BangEqualEqual : r.BangEqual : r.Bang,
          null
        );
        break;
      case "?":
        this.addToken(
          this.match("?") ? r.QuestionQuestion : this.match(".") ? r.QuestionDot : r.Question,
          null
        );
        break;
      case "=":
        if (this.match("=")) {
          this.addToken(
            this.match("=") ? r.EqualEqualEqual : r.EqualEqual,
            null
          );
          break;
        }
        this.addToken(
          this.match(">") ? r.Arrow : r.Equal,
          null
        );
        break;
      case "+":
        this.addToken(
          this.match("+") ? r.PlusPlus : this.match("=") ? r.PlusEqual : r.Plus,
          null
        );
        break;
      case "-":
        this.addToken(
          this.match("-") ? r.MinusMinus : this.match("=") ? r.MinusEqual : r.Minus,
          null
        );
        break;
      case "<":
        this.addToken(
          this.match("<") ? r.LeftShift : this.match("=") ? this.match(">") ? r.LessEqualGreater : r.LessEqual : r.Less,
          null
        );
        break;
      case ".":
        this.match(".") ? this.match(".") ? this.addToken(r.DotDotDot, null) : this.addToken(r.DotDot, null) : this.addToken(r.Dot, null);
        break;
      case "/":
        this.match("/") ? this.comment() : this.match("*") ? this.multilineComment() : this.addToken(
          this.match("=") ? r.SlashEqual : r.Slash,
          null
        );
        break;
      case "'":
      case '"':
      case "`":
        this.string(t);
        break;
      // ignore cases
      case `
`:
      case " ":
      case "\r":
      case "	":
        break;
      // complex cases
      default:
        q(t) ? this.number() : vt(t) ? this.identifier() : this.error(E.UNEXPECTED_CHARACTER, { char: t });
        break;
    }
  }
  error(t, e = {}) {
    throw new C(t, e, this.line, this.col);
  }
}
class b {
  constructor(t, e) {
    this.parent = t || null, this.values = e || {};
  }
  init(t) {
    this.values = t || {};
  }
  set(t, e) {
    this.values[t] = e;
  }
  get(t) {
    var s, n;
    if (typeof this.values[t] < "u")
      return this.values[t];
    const e = (n = (s = this.values) == null ? void 0 : s.constructor) == null ? void 0 : n.$imports;
    return e && typeof e[t] < "u" ? e[t] : this.parent !== null ? this.parent.get(t) : window[t];
  }
}
class Bt {
  constructor() {
    this.scope = new b(), this.scanner = new wt(), this.parser = new gt();
  }
  evaluate(t) {
    return t.result = t.accept(this);
  }
  visitPipelineExpr(t) {
    const e = this.evaluate(t.left);
    if (t.right instanceof J) {
      const n = this.evaluate(t.right.callee), a = [e];
      for (const o of t.right.args)
        o instanceof I ? a.push(...this.evaluate(o.value)) : a.push(this.evaluate(o));
      return t.right.callee instanceof R ? n.apply(t.right.callee.entity.result, a) : n(...a);
    }
    return this.evaluate(t.right)(e);
  }
  visitArrowFunctionExpr(t) {
    const e = this.scope;
    return (...s) => {
      const n = this.scope;
      this.scope = new b(e);
      for (let a = 0; a < t.params.length; a++)
        this.scope.set(t.params[a].lexeme, s[a]);
      try {
        return this.evaluate(t.body);
      } finally {
        this.scope = n;
      }
    };
  }
  error(t, e = {}, s, n) {
    throw new C(t, e, s, n);
  }
  visitVariableExpr(t) {
    return this.scope.get(t.name.lexeme);
  }
  visitAssignExpr(t) {
    const e = this.evaluate(t.value);
    return this.scope.set(t.name.lexeme, e), e;
  }
  visitKeyExpr(t) {
    return t.name.literal;
  }
  visitGetExpr(t) {
    const e = this.evaluate(t.entity), s = this.evaluate(t.key);
    if (!(!e && t.type === r.QuestionDot))
      return e[s];
  }
  visitSetExpr(t) {
    const e = this.evaluate(t.entity), s = this.evaluate(t.key), n = this.evaluate(t.value);
    return e[s] = n, n;
  }
  visitPostfixExpr(t) {
    const e = this.evaluate(t.entity), s = e + t.increment;
    if (t.entity instanceof O)
      this.scope.set(t.entity.name.lexeme, s);
    else if (t.entity instanceof R) {
      const n = new B(
        t.entity.entity,
        t.entity.key,
        new _(s, t.line),
        t.line
      );
      this.evaluate(n);
    } else
      this.error(E.INVALID_POSTFIX_LVALUE, { entity: t.entity }, t.line);
    return e;
  }
  visitListExpr(t) {
    const e = [];
    for (const s of t.value)
      s instanceof I ? e.push(...this.evaluate(s.value)) : e.push(this.evaluate(s));
    return e;
  }
  visitSpreadExpr(t) {
    return this.evaluate(t.value);
  }
  templateParse(t) {
    const e = this.scanner.scan(t), s = this.parser.parse(e);
    let n = "";
    for (const a of s)
      n += this.evaluate(a).toString();
    return n;
  }
  visitTemplateExpr(t) {
    return t.value.replace(
      /\{\{([\s\S]+?)\}\}/g,
      (s, n) => this.templateParse(n)
    );
  }
  visitBinaryExpr(t) {
    const e = this.evaluate(t.left), s = this.evaluate(t.right);
    switch (t.operator.type) {
      case r.Minus:
      case r.MinusEqual:
        return e - s;
      case r.Slash:
      case r.SlashEqual:
        return e / s;
      case r.Star:
      case r.StarEqual:
        return e * s;
      case r.Percent:
      case r.PercentEqual:
        return e % s;
      case r.Plus:
      case r.PlusEqual:
        return e + s;
      case r.Pipe:
        return e | s;
      case r.Caret:
        return e ^ s;
      case r.Greater:
        return e > s;
      case r.GreaterEqual:
        return e >= s;
      case r.Less:
        return e < s;
      case r.LessEqual:
        return e <= s;
      case r.EqualEqual:
      case r.EqualEqualEqual:
        return e === s;
      case r.BangEqual:
      case r.BangEqualEqual:
        return e !== s;
      case r.Instanceof:
        return e instanceof s;
      case r.In:
        return e in s;
      case r.LeftShift:
        return e << s;
      case r.RightShift:
        return e >> s;
      default:
        return this.error(E.UNKNOWN_BINARY_OPERATOR, { operator: t.operator }, t.line), null;
    }
  }
  visitLogicalExpr(t) {
    const e = this.evaluate(t.left);
    if (t.operator.type === r.Or) {
      if (e)
        return e;
    } else if (!e)
      return e;
    return this.evaluate(t.right);
  }
  visitTernaryExpr(t) {
    return this.evaluate(t.condition) ? this.evaluate(t.thenExpr) : this.evaluate(t.elseExpr);
  }
  visitNullCoalescingExpr(t) {
    const e = this.evaluate(t.left);
    return e ?? this.evaluate(t.right);
  }
  visitGroupingExpr(t) {
    return this.evaluate(t.expression);
  }
  visitLiteralExpr(t) {
    return t.value;
  }
  visitUnaryExpr(t) {
    const e = this.evaluate(t.right);
    switch (t.operator.type) {
      case r.Minus:
        return -e;
      case r.Bang:
        return !e;
      case r.Tilde:
        return ~e;
      case r.PlusPlus:
      case r.MinusMinus: {
        const s = Number(e) + (t.operator.type === r.PlusPlus ? 1 : -1);
        if (t.right instanceof O)
          this.scope.set(t.right.name.lexeme, s);
        else if (t.right instanceof R) {
          const n = new B(
            t.right.entity,
            t.right.key,
            new _(s, t.line),
            t.line
          );
          this.evaluate(n);
        } else
          this.error(
            E.INVALID_PREFIX_RVALUE,
            { right: t.right },
            t.line
          );
        return s;
      }
      default:
        return this.error(E.UNKNOWN_UNARY_OPERATOR, { operator: t.operator }, t.line), null;
    }
  }
  visitCallExpr(t) {
    const e = this.evaluate(t.callee);
    if (e == null && t.optional) return;
    typeof e != "function" && this.error(E.NOT_A_FUNCTION, { callee: e }, t.line);
    const s = [];
    for (const n of t.args)
      n instanceof I ? s.push(...this.evaluate(n.value)) : s.push(this.evaluate(n));
    return t.callee instanceof R ? e.apply(t.callee.entity.result, s) : e(...s);
  }
  visitNewExpr(t) {
    const e = this.evaluate(t.clazz);
    typeof e != "function" && this.error(
      E.NOT_A_CLASS,
      { clazz: e },
      t.line
    );
    const s = [];
    for (const n of t.args)
      s.push(this.evaluate(n));
    return new e(...s);
  }
  visitDictionaryExpr(t) {
    const e = {};
    for (const s of t.properties)
      if (s instanceof I)
        Object.assign(e, this.evaluate(s.value));
      else {
        const n = this.evaluate(s.key), a = this.evaluate(s.value);
        e[n] = a;
      }
    return e;
  }
  visitTypeofExpr(t) {
    return typeof this.evaluate(t.value);
  }
  visitEachExpr(t) {
    return [
      t.name.lexeme,
      t.key ? t.key.lexeme : null,
      this.evaluate(t.iterable)
    ];
  }
  visitVoidExpr(t) {
    return this.evaluate(t.value), "";
  }
  visitDebugExpr(t) {
    const e = this.evaluate(t.value);
    return console.log(e), "";
  }
}
class Q {
}
class ft extends Q {
  constructor(t, e, s, n, a = 0) {
    super(), this.type = "element", this.name = t, this.attributes = e, this.children = s, this.self = n, this.line = a;
  }
  accept(t, e) {
    return t.visitElementKNode(this, e);
  }
  toString() {
    return "KNode.Element";
  }
}
class Gt extends Q {
  constructor(t, e, s = 0) {
    super(), this.type = "attribute", this.name = t, this.value = e, this.line = s;
  }
  accept(t, e) {
    return t.visitAttributeKNode(this, e);
  }
  toString() {
    return "KNode.Attribute";
  }
}
class Vt extends Q {
  constructor(t, e = 0) {
    super(), this.type = "text", this.value = t, this.line = e;
  }
  accept(t, e) {
    return t.visitTextKNode(this, e);
  }
  toString() {
    return "KNode.Text";
  }
}
class Xt extends Q {
  constructor(t, e = 0) {
    super(), this.type = "doctype", this.value = t, this.line = e;
  }
  accept(t, e) {
    return t.visitDoctypeKNode(this, e);
  }
  toString() {
    return "KNode.Doctype";
  }
}
class et {
  parse(t) {
    for (this.current = 0, this.line = 1, this.col = 1, this.source = t, this.nodes = []; !this.eof(); ) {
      const e = this.node();
      e !== null && this.nodes.push(e);
    }
    return this.source = "", this.nodes;
  }
  match(...t) {
    for (const e of t)
      if (this.check(e))
        return this.current += e.length, !0;
    return !1;
  }
  advance(t = "") {
    this.eof() || (this.check(`
`) && (this.line += 1, this.col = 0), this.eof() ? this.error(E.UNEXPECTED_EOF, { eofError: t }) : this.current++);
  }
  peek(...t) {
    for (const e of t)
      if (this.check(e))
        return !0;
    return !1;
  }
  check(t) {
    return this.source.slice(this.current, this.current + t.length) === t;
  }
  eof() {
    return this.current > this.source.length;
  }
  error(t, e = {}) {
    throw new C(t, e, this.line, this.col);
  }
  node() {
    this.whitespace();
    let t;
    return this.match("</") && this.error(E.UNEXPECTED_CLOSING_TAG), this.match("<!--") ? t = this.comment() : this.match("<!doctype") || this.match("<!DOCTYPE") ? t = this.doctype() : this.match("<") ? t = this.element() : t = this.text(), this.whitespace(), t;
  }
  comment() {
    do
      this.advance("Expected comment closing '-->'");
    while (!this.match("-->"));
    return null;
  }
  doctype() {
    const t = this.current;
    do
      this.advance("Expected closing doctype");
    while (!this.match(">"));
    const e = this.source.slice(t, this.current - 1).trim();
    return new Xt(e, this.line);
  }
  element() {
    const t = this.line, e = this.identifier("/", ">");
    e || this.error(E.EXPECTED_TAG_NAME);
    const s = this.attributes();
    if (this.match("/>") || qt.includes(e) && this.match(">"))
      return new ft(e, s, [], !0, this.line);
    this.match(">") || this.error(E.EXPECTED_CLOSING_BRACKET);
    let n = [];
    return this.whitespace(), this.peek("</") || (n = this.children(e)), this.close(e), new ft(e, s, n, !1, t);
  }
  close(t) {
    this.match("</") || this.error(E.EXPECTED_CLOSING_TAG, { name: t }), this.match(`${t}`) || this.error(E.EXPECTED_CLOSING_TAG, { name: t }), this.whitespace(), this.match(">") || this.error(E.EXPECTED_CLOSING_TAG, { name: t });
  }
  children(t) {
    const e = [];
    do {
      this.eof() && this.error(E.EXPECTED_CLOSING_TAG, { name: t });
      const s = this.node();
      s !== null && e.push(s);
    } while (!this.peek("</"));
    return e;
  }
  attributes() {
    const t = [];
    for (; !this.peek(">", "/>") && !this.eof(); ) {
      this.whitespace();
      const e = this.line, s = this.identifier("=", ">", "/>");
      s || this.error(E.BLANK_ATTRIBUTE_NAME), this.whitespace();
      let n = "";
      this.match("=") && (this.whitespace(), this.match("'") ? n = this.decodeEntities(this.string("'")) : this.match('"') ? n = this.decodeEntities(this.string('"')) : n = this.decodeEntities(this.identifier(">", "/>"))), this.whitespace(), t.push(new Gt(s, n, e));
    }
    return t;
  }
  text() {
    const t = this.current, e = this.line;
    let s = 0;
    for (; !this.eof(); ) {
      if (this.match("{{")) {
        s++;
        continue;
      }
      if (s > 0 && this.match("}}")) {
        s--;
        continue;
      }
      if (s === 0 && this.peek("<"))
        break;
      this.advance();
    }
    const n = this.source.slice(t, this.current).trim();
    return n ? new Vt(this.decodeEntities(n), e) : null;
  }
  decodeEntities(t) {
    return t.replace(/&nbsp;/g, " ").replace(/&lt;/g, "<").replace(/&gt;/g, ">").replace(/&quot;/g, '"').replace(/&apos;/g, "'").replace(/&amp;/g, "&");
  }
  whitespace() {
    let t = 0;
    for (; this.peek(...ut) && !this.eof(); )
      t += 1, this.advance();
    return t;
  }
  identifier(...t) {
    this.whitespace();
    const e = this.current;
    for (; !this.peek(...ut, ...t); )
      this.advance(`Expected closing ${t}`);
    const s = this.current;
    return this.whitespace(), this.source.slice(e, s).trim();
  }
  string(t) {
    const e = this.current;
    for (; !this.match(t); )
      this.advance(`Expected closing ${t}`);
    return this.source.slice(e, this.current - 1);
  }
}
function Ht(i) {
  history.pushState(null, "", i), window.dispatchEvent(new PopStateEvent("popstate"));
}
function Qt(i, t) {
  if (i === "*") return {};
  const e = i.split("/").filter(Boolean), s = t.split("/").filter(Boolean);
  if (e.length !== s.length) return null;
  const n = {};
  for (let a = 0; a < e.length; a++)
    if (e[a].startsWith(":"))
      n[e[a].slice(1)] = s[a];
    else if (e[a] !== s[a])
      return null;
  return n;
}
class pt extends At {
  constructor() {
    super(...arguments), this.routes = [];
  }
  setRoutes(t) {
    this.routes = t;
  }
  onMount() {
    window.addEventListener("popstate", () => this._navigate(), {
      signal: this.$abortController.signal
    }), this._navigate();
  }
  async _navigate() {
    const t = window.location.pathname;
    for (const e of this.routes) {
      const s = Qt(e.path, t);
      if (s !== null) {
        if (e.guard && !await e.guard())
          return;
        this._mount(e.component, s);
        return;
      }
    }
  }
  _mount(t, e) {
    const s = this.ref;
    !s || !this.transpiler || this.transpiler.mountComponent(t, s, e);
  }
}
class j {
  constructor(t, e = "boundary") {
    this.start = document.createComment(`${e}-start`), this.end = document.createComment(`${e}-end`), t.insert && typeof t.insert == "function" ? (t.insert(this.start), t.insert(this.end)) : (t.appendChild(this.start), t.appendChild(this.end));
  }
  clear() {
    var e;
    let t = this.start.nextSibling;
    for (; t && t !== this.end; ) {
      const s = t;
      t = t.nextSibling, (e = s.parentNode) == null || e.removeChild(s);
    }
  }
  insert(t) {
    var e;
    (e = this.end.parentNode) == null || e.insertBefore(t, this.end);
  }
  nodes() {
    const t = [];
    let e = this.start.nextSibling;
    for (; e && e !== this.end; )
      t.push(e), e = e.nextSibling;
    return t;
  }
  get parent() {
    return this.start.parentNode;
  }
}
const G = /* @__PURE__ */ new Map(), Z = [];
let L = !1, X = !0;
function T() {
  L = !1;
  for (const [t, e] of G.entries())
    try {
      typeof t.onChanges == "function" && t.onChanges();
      for (const s of e)
        s();
      typeof t.onRender == "function" && t.onRender();
    } catch (s) {
      console.error("[Kasper] Error during component update:", s);
    }
  G.clear();
  const i = Z.splice(0);
  for (const t of i)
    try {
      t();
    } catch (e) {
      console.error("[Kasper] Error in nextTick callback:", e);
    }
}
function $(i, t) {
  if (!X) {
    t();
    return;
  }
  G.has(i) || G.set(i, []), G.get(i).push(t), L || (L = !0, queueMicrotask(T));
}
function M(i) {
  const t = X;
  X = !1;
  try {
    i();
  } finally {
    X = t;
  }
}
function Jt(i) {
  if (i) {
    Z.push(i), L || (L = !0, queueMicrotask(T));
    return;
  }
  return new Promise((t) => {
    Z.push(t), L || (L = !0, queueMicrotask(T));
  });
}
const dt = {
  esc: ["Escape", "Esc"],
  escape: ["Escape", "Esc"],
  space: [" ", "Spacebar"],
  up: ["ArrowUp", "Up"],
  down: ["ArrowDown", "Down"],
  left: ["ArrowLeft", "Left"],
  right: ["ArrowRight", "Right"],
  del: ["Delete", "Del"],
  delete: ["Delete", "Del"],
  ins: ["Insert"],
  dot: ["."],
  comma: [","],
  slash: ["/"],
  backslash: ["\\"],
  plus: ["+"],
  minus: ["-"],
  equal: ["="]
};
class bt {
  constructor(t) {
    this.scanner = new wt(), this.parser = new gt(), this.templateParser = new et(), this.interpreter = new Bt(), this.registry = {}, this.mode = "development", this.isRendering = !1, this.registry.router = { component: pt }, t && t.registry && (this.registry = { ...this.registry, ...t.registry });
  }
  renderComponentInstance(t, e, s, n, a) {
    a && (t.$slots = a), t.$render = () => {
      this.isRendering = !0;
      try {
        this.destroy(s), s.innerHTML = "";
        const c = new b(n, t);
        c.set("$instance", t), a && (t.$slots = a);
        const m = this.interpreter.scope;
        this.interpreter.scope = c, M(() => {
          this.createSiblings(e, s), typeof t.onRender == "function" && t.onRender();
        }), this.interpreter.scope = m;
      } finally {
        this.isRendering = !1;
      }
    }, typeof t.onMount == "function" && t.onMount();
    const o = new b(n, t);
    o.set("$instance", t), this.interpreter.scope = o, M(() => {
      this.createSiblings(e, s), typeof t.onRender == "function" && t.onRender();
    }), this.interpreter.scope = n;
  }
  resolveNodes(t) {
    const e = this.registry[t];
    if (e.nodes !== void 0) return e.nodes;
    const s = e.component.template;
    return s ? (e.nodes = this.templateParser.parse(s), e.nodes) : (e.nodes = [], e.nodes);
  }
  evaluate(t, e) {
    if (t.type === "element") {
      const s = t, n = this.findAttr(s, ["@elseif", "@else"]);
      if (n) {
        const a = n.name.startsWith("@") ? n.name.slice(1) : n.name;
        this.error(E.MISPLACED_CONDITIONAL, { name: a }, s.name);
      }
    }
    t.accept(this, e);
  }
  bindMethods(t) {
    var s;
    if (!t || typeof t != "object") return;
    let e = Object.getPrototypeOf(t);
    for (; e && e !== Object.prototype; ) {
      for (const n of Object.getOwnPropertyNames(e))
        (s = Object.getOwnPropertyDescriptor(e, n)) != null && s.get || typeof t[n] == "function" && n !== "constructor" && !Object.prototype.hasOwnProperty.call(t, n) && (t[n] = t[n].bind(t));
      e = Object.getPrototypeOf(e);
    }
  }
  // Creates an effect that restores the current scope on every re-run,
  // so effects set up inside @each always evaluate in their item scope.
  scopedEffect(t) {
    const e = this.interpreter.scope;
    return tt(() => {
      const s = this.interpreter.scope;
      this.interpreter.scope = e;
      try {
        t();
      } finally {
        this.interpreter.scope = s;
      }
    });
  }
  // Wraps a refresh function so it restores the correct scope when called
  // directly by triggerRefresh (outside of the signal effect machinery).
  scopedRefresh(t) {
    const e = this.interpreter.scope;
    return () => {
      const s = this.interpreter.scope;
      this.interpreter.scope = e;
      try {
        t();
      } finally {
        this.interpreter.scope = s;
      }
    };
  }
  // evaluates expressions and returns the result of the first evaluation
  execute(t, e) {
    const s = this.scanner.scan(t), n = this.parser.parse(s), a = this.interpreter.scope;
    e && (this.interpreter.scope = e);
    const o = n.map(
      (c) => this.interpreter.evaluate(c)
    );
    return this.interpreter.scope = a, o && o.length ? o[o.length - 1] : void 0;
  }
  transpile(t, e, s) {
    this.isRendering = !0;
    try {
      return this.destroy(s), s.innerHTML = "", this.bindMethods(e), this.interpreter.scope.init(e), this.interpreter.scope.set("$instance", e), M(() => {
        this.createSiblings(t, s), this.triggerRender();
      }), s;
    } finally {
      this.isRendering = !1;
    }
  }
  visitElementKNode(t, e) {
    this.createElement(t, e);
  }
  visitTextKNode(t, e) {
    const s = document.createTextNode("");
    e && (e.insert && typeof e.insert == "function" ? e.insert(s) : e.appendChild(s));
    const n = this.scopedEffect(() => {
      const a = this.evaluateTemplateString(t.value), o = this.interpreter.scope.get("$instance");
      o ? $(o, () => {
        s.textContent = a;
      }) : s.textContent = a;
    });
    this.trackEffect(s, n);
  }
  visitAttributeKNode(t, e) {
    const s = document.createAttribute(t.name), n = this.scopedEffect(() => {
      s.value = this.evaluateTemplateString(t.value);
    });
    this.trackEffect(s, n), e && e.setAttributeNode(s);
  }
  visitCommentKNode(t, e) {
  }
  trackEffect(t, e) {
    t.$kasperEffects || (t.$kasperEffects = []), t.$kasperEffects.push(e);
  }
  findAttr(t, e) {
    if (!t || !t.attributes || !t.attributes.length)
      return null;
    const s = t.attributes.find(
      (n) => e.includes(n.name)
    );
    return s || null;
  }
  doIf(t, e) {
    const s = new j(e, "if"), n = () => {
      const o = this.interpreter.scope.get("$instance"), c = o ? new b(this.interpreter.scope) : this.interpreter.scope, m = this.interpreter.scope;
      this.interpreter.scope = c;
      const d = [];
      if (d.push(!!this.execute(t[0][1].value)), !d[0]) {
        for (const p of t.slice(1))
          if (this.findAttr(p[0], ["@elseif"])) {
            const u = !!this.execute(p[1].value);
            if (d.push(u), u) break;
          } else if (this.findAttr(p[0], ["@else"])) {
            d.push(!0);
            break;
          }
      }
      this.interpreter.scope = m;
      const l = () => {
        s.nodes().forEach((u) => this.destroyNode(u)), s.clear();
        const p = this.interpreter.scope;
        this.interpreter.scope = c;
        try {
          if (d[0]) {
            t[0][0].accept(this, s);
            return;
          }
          for (let u = 1; u < d.length; u++)
            if (d[u]) {
              t[u][0].accept(this, s);
              return;
            }
        } finally {
          this.interpreter.scope = p;
        }
      };
      o ? $(o, l) : l();
    };
    s.start.$kasperRefresh = this.scopedRefresh(n);
    const a = this.scopedEffect(n);
    this.trackEffect(s, a);
  }
  doEach(t, e, s) {
    const n = this.findAttr(e, ["@key"]);
    n ? this.doEachKeyed(t, e, s, n) : this.doEachUnkeyed(t, e, s);
  }
  doEachUnkeyed(t, e, s) {
    const n = new j(s, "each"), a = this.interpreter.scope, o = () => {
      const m = this.scanner.scan(t.value), [d, l, p] = this.interpreter.evaluate(
        this.parser.foreach(m)
      ), u = this.interpreter.scope.get("$instance"), f = () => {
        n.nodes().forEach((w) => this.destroyNode(w)), n.clear();
        let h = 0;
        for (const w of p) {
          const g = { [d]: w };
          l && (g[l] = h), this.interpreter.scope = new b(a, g), this.createElement(e, n), h += 1;
        }
        this.interpreter.scope = a;
      };
      u ? $(u, f) : f();
    };
    n.start.$kasperRefresh = this.scopedRefresh(o);
    const c = this.scopedEffect(o);
    this.trackEffect(n, c);
  }
  triggerRefresh(t) {
    var e;
    t.$kasperRefresh && t.$kasperRefresh(), t.$kasperEffects && t.$kasperEffects.forEach((s) => {
      typeof s.run == "function" && s.run();
    }), (e = t.childNodes) == null || e.forEach((s) => this.triggerRefresh(s));
  }
  doEachKeyed(t, e, s, n) {
    const a = new j(s, "each"), o = this.interpreter.scope, c = /* @__PURE__ */ new Map(), m = () => {
      const l = this.scanner.scan(t.value), [p, u, f] = this.interpreter.evaluate(
        this.parser.foreach(l)
      ), h = this.interpreter.scope.get("$instance"), w = [], g = /* @__PURE__ */ new Set();
      let A = 0;
      for (const N of f) {
        const P = { [p]: N };
        u && (P[u] = A), this.interpreter.scope = new b(o, P);
        const S = this.execute(n.value);
        this.mode === "development" && g.has(S) && console.warn(`[Kasper] Duplicate key detected in @each: "${S}". Keys must be unique to ensure correct reconciliation.`), g.add(S), w.push({ item: N, idx: A, key: S }), A++;
      }
      const y = () => {
        var st;
        const N = new Set(w.map((k) => k.key));
        for (const [k, K] of c)
          N.has(k) || (this.destroyNode(K), (st = K.parentNode) == null || st.removeChild(K), c.delete(k));
        const P = a.end.parentNode;
        let S = a.start;
        for (const { item: k, idx: K, key: W } of w) {
          const it = { [p]: k };
          if (u && (it[u] = K), this.interpreter.scope = new b(o, it), c.has(W)) {
            const x = c.get(W);
            S.nextSibling !== x && P.insertBefore(x, S.nextSibling), S = x;
            const F = x.$kasperScope;
            F && (F.set(p, k), u && F.set(u, K), this.triggerRefresh(x));
          } else {
            const x = this.createElement(e, a);
            x && (S.nextSibling !== x && P.insertBefore(x, S.nextSibling), S = x, c.set(W, x), x.$kasperScope = this.interpreter.scope);
          }
        }
        this.interpreter.scope = o;
      };
      h ? $(h, y) : y();
    };
    a.start.$kasperRefresh = this.scopedRefresh(m);
    const d = this.scopedEffect(m);
    this.trackEffect(a, d);
  }
  createSiblings(t, e) {
    let s = 0;
    const n = this.interpreter.scope;
    let a = null;
    for (; s < t.length; ) {
      const o = t[s++];
      if (o.type === "element") {
        const c = o, m = this.findAttr(c, ["@let"]);
        m && (a || (a = new b(n), this.interpreter.scope = a), this.execute(m.value));
        const d = this.findAttr(c, ["@if"]), l = this.findAttr(c, ["@elseif"]), p = this.findAttr(c, ["@else"]), u = this.findAttr(c, ["@each"]);
        if (this.mode === "development" && [d, l, p, u].filter((h) => h).length > 1 && this.error(E.MULTIPLE_STRUCTURAL_DIRECTIVES, {}, c.name), u) {
          this.doEach(u, c, e);
          continue;
        }
        if (d) {
          const f = [[c, d]];
          for (; s < t.length; ) {
            const h = t[s];
            if (h.type !== "element") break;
            const w = this.findAttr(h, [
              "@else",
              "@elseif"
            ]);
            if (w)
              f.push([h, w]), s += 1;
            else
              break;
          }
          this.doIf(f, e);
          continue;
        }
      }
      this.evaluate(o, e);
    }
    this.interpreter.scope = n;
  }
  createElement(t, e) {
    var s, n;
    try {
      if (t.name === "slot") {
        const l = this.findAttr(t, ["@name"]), p = l ? l.value : "default", u = this.interpreter.scope.get("$slots");
        if (u && u[p]) {
          const f = this.interpreter.scope;
          u[p].scope && (this.interpreter.scope = u[p].scope), this.createSiblings(u[p], e), this.interpreter.scope = f;
        }
        return;
      }
      const a = t.name === "void", o = !!this.registry[t.name], c = a ? e : document.createElement(t.name), m = this.interpreter.scope;
      if (c && c !== e && this.interpreter.scope.set("$ref", c), o) {
        let l = {};
        const p = t.attributes.filter(
          (h) => h.name.startsWith("@:")
        ), u = this.createComponentArgs(p), f = { default: [] };
        f.default.scope = this.interpreter.scope;
        for (const h of t.children) {
          if (h.type === "element") {
            const w = this.findAttr(h, ["@slot"]);
            if (w) {
              const g = w.value;
              f[g] || (f[g] = [], f[g].scope = this.interpreter.scope), f[g].push(h);
              continue;
            }
          }
          f.default.push(h);
        }
        if ((s = this.registry[t.name]) != null && s.lazy) {
          const h = this.registry[t.name];
          if (h.fallback) {
            const w = this.templateParser.parse(h.fallback.template ?? ""), g = new h.fallback({ args: {}, ref: c, transpiler: this });
            this.bindMethods(g), c.$kasperInstance = g, this.renderComponentInstance(g, w, c, m);
          }
          return h._promise || (h._promise = h.component().then((w) => {
            h.nodes = this.templateParser.parse(w.template ?? ""), h.component = w, delete h.lazy, delete h._promise;
          })), h._promise.then(() => {
            this.destroy(c), c.innerHTML = "";
            const w = h.component, g = new w({ args: u, ref: c, transpiler: this });
            this.bindMethods(g), c.$kasperInstance = g, this.renderComponentInstance(g, h.nodes, c, m, f);
          }), e && (e.insert && typeof e.insert == "function" ? e.insert(c) : e.appendChild(c)), c;
        }
        if ((n = this.registry[t.name]) != null && n.component) {
          if (l = new this.registry[t.name].component({
            args: u,
            ref: c,
            transpiler: this
          }), this.bindMethods(l), c.$kasperInstance = l, t.name === "router" && l instanceof pt) {
            const h = new b(m, l);
            l.setRoutes(this.extractRoutes(t.children, void 0, h));
          }
          this.renderComponentInstance(l, this.resolveNodes(t.name), c, m, f);
        }
        return e && (e.insert && typeof e.insert == "function" ? e.insert(c) : e.appendChild(c)), c;
      }
      if (!a) {
        const l = t.attributes.filter(
          (f) => f.name.startsWith("@on:")
        );
        for (const f of l)
          this.createEventListener(c, f);
        const p = t.attributes.filter(
          (f) => !f.name.startsWith("@")
        );
        for (const f of p)
          this.evaluate(f, c);
        const u = t.attributes.filter((f) => {
          const h = f.name;
          return h.startsWith("@") && !["@if", "@elseif", "@else", "@each", "@let", "@key", "@ref"].includes(
            h
          ) && !h.startsWith("@on:") && !h.startsWith("@:");
        });
        for (const f of u) {
          const h = f.name.slice(1);
          if (h === "class") {
            const w = this.scopedEffect(() => {
              const g = this.execute(f.value), A = this.interpreter.scope.get("$instance"), y = () => {
                c.setAttribute("class", g);
              };
              A ? $(A, y) : y();
            });
            this.trackEffect(c, w);
          } else {
            const w = this.scopedEffect(() => {
              const g = this.execute(f.value), A = this.interpreter.scope.get("$instance"), y = () => {
                if (g === !1 || g === null || g === void 0)
                  h !== "style" && c.removeAttribute(h);
                else if (h === "style") {
                  const N = c.getAttribute("style"), P = N && !N.includes(g) ? `${N.endsWith(";") ? N : N + ";"} ${g}` : g;
                  c.setAttribute("style", P);
                } else
                  c.setAttribute(h, g);
              };
              A ? $(A, y) : y();
            });
            this.trackEffect(c, w);
          }
        }
      }
      e && !a && (e.insert && typeof e.insert == "function" ? e.insert(c) : e.appendChild(c));
      const d = this.findAttr(t, ["@ref"]);
      if (d && !a) {
        const l = d.value.trim(), p = this.interpreter.scope.get("$instance");
        p ? p[l] = c : this.interpreter.scope.set(l, c);
      }
      return t.self || (this.createSiblings(t.children, c), this.interpreter.scope = m), c;
    } catch (a) {
      if (a instanceof C) throw a.withTag(t.name);
      this.error(E.RUNTIME_ERROR, { message: a.message || `${a}` }, t.name);
    }
  }
  createComponentArgs(t) {
    if (!t.length)
      return {};
    const e = {};
    for (const s of t) {
      const n = s.name.split(":")[1];
      if (this.mode === "development" && n.toLowerCase().startsWith("on")) {
        const a = s.value.trim();
        /^[\w$.][\w$.]*\s*\(.*\)\s*$/.test(a) && !a.includes("=>") && console.warn(
          `[Kasper] @:${n}="${s.value}" — the expression is called during render and its return value is passed as the prop. If it returns a function, that function becomes the handler (factory pattern). If it returns undefined, the prop receives undefined. If the function has reactive side effects, ensure it does not both read and write the same signal.`
        );
      }
      e[n] = this.execute(s.value);
    }
    return e;
  }
  createEventListener(t, e) {
    const [s, ...n] = e.name.split(":")[1].split("."), a = new b(this.interpreter.scope), o = this.interpreter.scope.get("$instance"), c = {};
    o && o.$abortController && (c.signal = o.$abortController.signal), n.includes("once") && (c.once = !0), n.includes("passive") && (c.passive = !0), n.includes("capture") && (c.capture = !0);
    const m = ["prevent", "stop", "once", "passive", "capture", "ctrl", "shift", "alt", "meta"], d = n.filter((l) => !m.includes(l.toLowerCase()));
    t.addEventListener(
      s,
      (l) => {
        d.length > 0 && !d.some((u) => {
          var h;
          const f = u.toLowerCase();
          return !!(dt[f] && dt[f].includes(l.key) || f === ((h = l.key) == null ? void 0 : h.toLowerCase()));
        }) || n.includes("ctrl") && !l.ctrlKey || n.includes("shift") && !l.shiftKey || n.includes("alt") && !l.altKey || n.includes("meta") && !l.metaKey || (n.includes("prevent") && l.preventDefault(), n.includes("stop") && l.stopPropagation(), a.set("$event", l), this.execute(e.value, a));
      },
      c
    );
  }
  evaluateTemplateString(t) {
    return t && (/\{\{.+\}\}/ms.test(t) ? t.replace(/\{\{([\s\S]+?)\}\}/g, (s, n) => this.evaluateExpression(n)) : t);
  }
  evaluateExpression(t) {
    const e = this.scanner.scan(t), s = this.parser.parse(e);
    let n = "";
    for (const a of s)
      n += `${this.interpreter.evaluate(a)}`;
    return n;
  }
  destroyNode(t) {
    var e;
    if (t.$kasperInstance) {
      const s = t.$kasperInstance;
      s.onDestroy && s.onDestroy(), s.$abortController && s.$abortController.abort();
    }
    if (t.$kasperEffects && (t.$kasperEffects.forEach((s) => s()), t.$kasperEffects = []), t.attributes)
      for (let s = 0; s < t.attributes.length; s++) {
        const n = t.attributes[s];
        n.$kasperEffects && (n.$kasperEffects.forEach((a) => a()), n.$kasperEffects = []);
      }
    (e = t.childNodes) == null || e.forEach((s) => this.destroyNode(s));
  }
  destroy(t) {
    t.childNodes.forEach((e) => this.destroyNode(e));
  }
  mountComponent(t, e, s = {}) {
    this.destroy(e), e.innerHTML = "";
    const n = t.template;
    if (!n) return;
    const a = this.templateParser.parse(n), o = document.createElement("div");
    e.appendChild(o);
    const c = new t({ args: { params: s }, ref: o, transpiler: this });
    this.bindMethods(c), o.$kasperInstance = c;
    const m = a;
    c.$render = () => {
      this.isRendering = !0;
      try {
        this.destroy(o), o.innerHTML = "";
        const p = new b(null, c);
        p.set("$instance", c);
        const u = this.interpreter.scope;
        this.interpreter.scope = p, M(() => {
          this.createSiblings(m, o), typeof c.onRender == "function" && c.onRender();
        }), this.interpreter.scope = u;
      } finally {
        this.isRendering = !1;
      }
    };
    const d = new b(null, c);
    d.set("$instance", c);
    const l = this.interpreter.scope;
    this.interpreter.scope = d, M(() => {
      this.createSiblings(a, o);
    }), this.interpreter.scope = l, typeof c.onMount == "function" && c.onMount(), typeof c.onRender == "function" && c.onRender();
  }
  extractRoutes(t, e, s) {
    const n = [], a = s ? this.interpreter.scope : void 0;
    s && (this.interpreter.scope = s);
    for (const o of t) {
      if (o.type !== "element") continue;
      const c = o;
      if (c.name === "route") {
        const m = this.findAttr(c, ["@path"]), d = this.findAttr(c, ["@component"]), l = this.findAttr(c, ["@guard"]);
        (!m || !d) && this.error(E.MISSING_REQUIRED_ATTR, { message: "<route> requires @path and @component attributes." }, c.name);
        const p = m.value, u = this.execute(d.value), f = l ? this.execute(l.value) : e;
        n.push({ path: p, component: u, guard: f });
      } else if (c.name === "guard") {
        const m = this.findAttr(c, ["@check"]);
        if (m || this.error(E.MISSING_REQUIRED_ATTR, { message: "<guard> requires @check attribute." }, c.name), !m) continue;
        const d = this.execute(m.value);
        n.push(...this.extractRoutes(c.children, d));
      }
    }
    return s && (this.interpreter.scope = a), n;
  }
  triggerRender() {
    if (this.isRendering) return;
    const t = this.interpreter.scope.get("$instance");
    t && typeof t.onRender == "function" && t.onRender();
  }
  visitDoctypeKNode(t) {
  }
  error(t, e, s) {
    let n = e;
    throw typeof e == "string" && (n = { message: e.includes("Runtime Error") ? e.replace("Runtime Error: ", "") : e }), new C(t, n, void 0, void 0, s);
  }
}
function Zt(i) {
  return {
    lazy: !0,
    component: () => i().then((t) => Object.values(t)[0])
  };
}
function Tt(i) {
  const t = new et();
  try {
    const e = t.parse(i);
    return JSON.stringify(e);
  } catch (e) {
    return JSON.stringify([e instanceof Error ? e.message : String(e)]);
  }
}
function te(i, t, e, s) {
  const a = new et().parse(i);
  return new bt({ registry: s || {} }).transpile(a, t || {}, e);
}
function Wt(i, t) {
  const e = document.createElement(t), s = new i.registry[t].component({
    ref: e,
    transpiler: i,
    args: {}
  });
  return {
    node: e,
    instance: s,
    nodes: i.resolveNodes(t)
  };
}
function ee(i) {
  const t = typeof i.root == "string" ? document.querySelector(i.root) : i.root;
  if (!t)
    throw new C(
      E.ROOT_ELEMENT_NOT_FOUND,
      { root: i.root }
    );
  const e = i.entry || "kasper-app";
  if (!i.registry[e])
    throw new C(
      E.ENTRY_COMPONENT_NOT_FOUND,
      { tag: e }
    );
  const s = new bt({ registry: i.registry });
  i.mode && (s.mode = i.mode);
  const { node: n, instance: a, nodes: o } = Wt(s, e);
  return t.innerHTML = "", t.appendChild(n), typeof a.onMount == "function" && a.onMount(), s.transpile(o, a, n), typeof a.onRender == "function" && a.onRender(), a;
}
export {
  ee as App,
  At as Component,
  gt as ExpressionParser,
  Bt as Interpreter,
  pt as Router,
  wt as Scanner,
  et as TemplateParser,
  bt as Transpiler,
  jt as batch,
  Nt as computed,
  tt as effect,
  Tt as execute,
  Zt as lazy,
  Ht as navigate,
  Jt as nextTick,
  Ft as signal,
  te as transpile,
  zt as watch
};
